# mini4wd.parts — Product & Technical Plan

Status: draft v2, 2026-09-08. The catalog pipeline is built and 382 parts, 8 chassis and 305 kits are committed; §6 was re-targeted the same day around an MVP builder with a live 3D view, and M1a (kits) closed the same day. i18n landed 2026-09-09, leaving PostHog as the only open M0 item at that point. The builder spine — bare-chassis entry, slot list, part picker — landed 2026-09-09 and re-ordered M1b (§6). Two owner decisions the same day: the 3D model becomes the selection surface rather than a view of it (§1, §5.4, §6 M1c), and the picker shows Tamiya's official product images rather than only linking to them (§1, §6 M1b). The loadout-label localisation was scoped on 2026-09-09 and turned out to be four problems wearing one field's name (§4.8); it landed the same day, together with the three-state origin badge. The kit entry point followed on 2026-09-09: `/build` now opens on a searchable, image-led picker over all 305 kits with the bare chassis behind a toggle, and it is the first surface to render Fandom-sourced content, so it carries the CC-BY-SA credit. It also cost 2× the budgeted payload for a reason worth reading (§6 M1b) and surfaced that CI checks nothing (§6 M0). CI checks tests, types (`scripts/` and `app/`) and the catalog before every deploy since 2026-09-16, leaving PostHog as the only open M0 item (§6 M0). The same day the owner took build cost out of the M1 done-criterion: prices differ by region, so a JPY/HKD total would be wrong for most readers (§6 M1b). M1c was scoped the same day as a no-mesh spike first (§5.5): the risks are Nuxt, bytes and touch, not Blender, and measuring TresJS against plain three settled the stack: plain three, no module, and the same component retires model-viewer once it can draw a real car (§4.1, §5.5). The spike landed the same day: `/build` draws MA as tappable proxies, and the measurements under §5.5 changed the body's hit volume, the home camera and the reset button. The same day the owner simplified the MVP around one stable screen: the builder is the home page (`/build` redirects there, and the model-viewer landing page is gone), the 3D pane and the build list are always shown — an empty MA proxy car before anything is chosen, a placeholder in the frame for chassis the pane cannot draw — and the kit/chassis picker opens from a 套件 row at the top of the list. Taken out as noise for a beginner: prices and packet quantity on part rows (the JPY sort tiebreaker stays, unseen), the origin badge (§4.8's three states still drive the scene's colours), the stock-shopping-list note, the race-class selector (the builder checks against Open), and rows no catalog part can fill (the switch). Front and rear wheel and tire rows gained a copy-from-the-other-end button. Product pictures were settled on 2026-09-16 and reversed the 2026-09-09 hotlink decision: every picture is now a 160×120 thumbnail we generated and serve ourselves, no page references Tamiya's CDN, and rows with no photo — most of a stock build — draw our own slot icons instead (§1 decision 4, §3.3, §6 M1b, §7). Part pages followed the same day, and left 764 routes that nothing links to: the browse indexes, chassis pages and breadcrumbs that close that were scoped on 2026-09-16 with six owner decisions, the load-bearing one being that the detail URLs do **not** move under their category — GitHub Pages cannot 301, and `BreadcrumbList` is JSON-LD, so the hierarchy is declared rather than routed (§4.6, §6 M1b); it landed the same day. Three smaller changes closed the day, also under §6 M1b: the HK/TW wording toggle was withdrawn (§4.1); the builder's base row shows the chassis and the box art moved to the body slot; and the five single-shaft chassis now name their stock propeller shaft as a real AO part. The shapes question was settled the same day (§5.6): parts and bodies are three.js geometry generated in the browser from catalog specs, not Blender models and not build123d exports, because the only reference is one product photo per part and the pane's job is an impression of the build, not a reproduction; Blender is held back for the chassis, and only as a committed script. Bodies were refined the same day: a shell should briefly resemble that kit's box art, so the three generic archetypes became per-kit silhouettes, a dozen numbers each through one loft, shared across recolours (§5.4, §5.6). The chassis question closed the same day (§5.6): all eight chassis are drawn in M1, from the same primitive helper that drew MA, at the detail a reader recognises from below and with the shell lifted; no chassis becomes a GLB and the socket tables are permanent, which retires the Blender pipeline and the slot-named-empties design in §5.2 entirely. On 2026-09-18 the owner asked for a body shell on every kit, moving the silhouettes outside MA from M3 into M1c: about 196 shapes for 305 kits, recognisable to a reader who owns the box, one colour each, planned in four phases under §5.6; all four landed the same day, and 176 shells now cover every kit. The wheels under them followed the same day (§5.6): every kit and every chassis drew one ⌀20 rim with a 6 mm band, so the 43 wheel and tire phrases the kits carry became a shape table in the 3D chunk — size, profile and face, no tread — keyed by the phrase itself so nothing is stored twice; the 53 wheel and tire parts resolve into the same table from their product names, and the builder stays the only surface for them (a part-page preview stays M3). The same day the pickers stopped opening on O-rings and spare gears: 25 add-ons now rank in one block under a divider, and four misfiled parts found their categories (§6 M1, after the M1 run). Later the same day the three wheel-family misfiles followed, and the base row's gear ratio started following the gears actually in the car instead of always printing the kit's (§6 M1b, Totals).
Research notes that fed this document (concepts, data sources, 3D assets, tech stack) are summarised inline with source links.

---

## 1. Vision and scope

A Traditional-Chinese-first (English second, Japanese later) site for Mini 4WD beginners that combines:

| # | Feature | Milestone |
|---|---|---|
| F1 | Parts database (chassis, kits, Grade-Up Parts) with chassis compatibility and class legality | M1 |
| F2 | Tutorials: how to pick parts for different goals, class glossary (Open / Stock / B-MAX) | M2 |
| F3 | Car builder: start from a **kit** or a **bare chassis**, fill slots with parts, see compatibility warnings | **M1** |
| F4 | Shareable builds with part list, permalink and preview image | M1 (URL-encoded) → M2 (short links + OG image) → M3 (3D preview) |
| F5 | Beginner wizard / recommender (budget, class, course type → suggested build) | M4 |
| F6 | Ratings and popularity for parts and builds | M2 |
| F7 | 3D view of the assembled car, re-rendering live as parts change, **and the surface you click to change it** | **M1** (owner moved click-to-select out of M3 on 2026-09-09) |
| F8 | Accounts, garages, comments, community data contributions | M4 |

Nothing like this exists today. Japanese resources are blog/wiki style and stale, Tamiya's compatibility matrix is a GIF image, the licensed Bandai game (超速グランプリ) shut down in May 2024, and there is no Traditional Chinese item-level database at all. See the prior-art notes in section 8.

### Guiding decisions

1. **Data first, 3D second — and the data is now paid for.** The builder, wizard, share pages and 3D view all sit on the same slot-graph data model, which landed on 2026-09-08 (382 parts, 8 chassis, a slot profile per chassis family). The ordering was never "3D last", it was "3D not before the data"; that condition is now met, so the 3D **view** moves into the MVP (§6 M1). ~~What stays late is 3D as an *input* surface — clicking the car to select parts — because that is the expensive, risky half and the 2D list already does the job better on mobile.~~ **Reversed 2026-09-09 by the owner: the 3D model is the selection surface, not a view of what a list chose.** The cost and the mobile/accessibility arguments are not disputed by this — they are now costs to pay rather than reasons to defer. The 2D list stays and is not scaffolding: 3D is primary and the first-sight UX, the list is the accessibility, efficiency and preference path, and none of those three jobs expires (§5.4). What is temporary is only that the list is also the *sole* surface until 3D covers more than one chassis.
2. **Catalog in git, user data in a database.** Parts, chassis, rules and guides are versioned YAML/Markdown in the repo. Only builds, votes and view counts live in a database.
3. **Stay in the Nuxt/Vue toolchain**, at near-zero hosting cost.
4. **Facts are ours, Tamiya's expression is not.** Store facts (item numbers, prices, dimensions, compatibility) and our own text, photos and simplified 3D shapes. Link out to Tamiya for official descriptions. **Amended 2026-09-09 by the owner: official product images are shown in the picker rather than only linked** — a part a beginner cannot recognise by name is not really in the picker at all. ~~The rule that does not move is that we never re-host them: `officialImage` is Tamiya's own CDN URL, stored as a fact and referenced from there.~~ **Amended again 2026-09-16 by the owner: the pictures are our own downscaled thumbnails, served from our origin, and nothing is hotlinked** (§6 M1b). The reasoning is not legal but practical — a hotlink spends Tamiya's bandwidth on every page view and puts our domain in their logs daily, which is what invites a block, while a copy costs them one fetch. The size is what keeps the copies defensible: 160×120, credited on the page, beside a link to Tamiya's product page, in one directory that a takedown deletes. See §7.

---

## 2. Domain summary (what the site must model)

### 2.1 Race classes

| Class | Who runs it | Key rules | Site treatment |
|---|---|---|---|
| **Open** (公認競技会規則) | Tamiya official (Japan Cup, TMAC, HK Cup) | 105×165×70 mm, ≥90 g with batteries, tires 22–35 mm, Tamiya parts only, 2×AA Tamiya/FDK. Single-shaft chassis: Tune 2 series, Light-Dash, Hyper-Dash 3, Power-Dash, Sprint-Dash. PRO chassis: PRO motors. Ultra/Plasma-Dash banned. | Baseline ruleset every build is checked against |
| **Stock Class** (ストッククラス / 基礎賽) | Tamiya official, since Apr 2025, revised 2026-05 | Open rules plus: no chassis cutting, no new holes, no modified parts, plates must not wobble, forbidden zone between axles. Dash motors allowed. | Primary beginner ruleset |
| **B-MAX GP** | Basic-MAX GP committee (Yokohama), Tamiya cooperates; popular in Taiwan (TMU league), Malaysia, UK | Unified with Stock Class in Dec 2025; ver5.0 effective 2026-06-01 is identical in the modification section. Factory roller combos only, gear pairings per chassis, no 提灯/catcher dampers. | Same ruleset as Stock with a few extra flags |
| **"B-Stock"** | Local shorthand; **confirmed by the site owner (2026-09-08) to mean Stock Class** | Same rules as Tamiya Stock Class, and therefore B-MAX GP as well since the two were unified in Dec 2025. Not to be confused with "Box Stock" (unmodified kit, normal motor), a stricter shop-defined class in PH/UK/MY. | Alias entry redirecting to Stock Class, with a note that organiser PDFs govern |
| Junior / Family / Trial | Tamiya official, by age | Tune-series motors only | Motor legality flag |
| GT Advance, Box Stock, regional Stock variants (TH/PH/VN) | Community / shops | Vary | Glossary entries only |

Sources: [Tamiya EN regulation](https://www.tamiya.com/english/mini4wd/regulation.htm), [Stock Class EN](https://www.tamiya.com/english/mini4wd/regulation_stockclass.html), [Tamiya HK 基礎賽](https://tamiya.hk/rule-stock-class/), [B-MAX GP ver5.0](https://basic-max.com/2026/05/13/bmaxgp-regu-v5/), [Japan Cup 2026](https://www.tamiya.com/japan/mini4wd/japancup.html).

### 2.2 Chassis

Current production chassis to feature in v1 (seven, plus ME as newest PRO sibling):

| Chassis | Motor | Shaft | Since | Kit gear | Notes |
|---|---|---|---|---|---|
| MA | mid | double (PRO) | 2013 | 3.5:1 | Most kits (69), monocoque, 6 rollers, beginner-friendly |
| MS | mid | double (PRO) | 2005 | 4:1 | 3-piece; basis of MSフレキ (Open only) |
| ME | mid | double (PRO) | 2025/26 | 3.5:1 | Newest; added to official regs Jan 2026 |
| AR | rear | single | 2012 | 4.2:1 | First 6-roller chassis, easy maintenance |
| FM-A | front | single | 2017 | 3.5:1 | Front-motor, stable on undulating tracks |
| VZ | rear | single | 2020 | 3.5:1 | Lightest (108 g), successor to VS |
| Super-II | rear | single | 2010 | 4.2:1 | Still in yearly limited kits |
| VS / Super XX / Super FM | rear/rear/front | single | 1997–2009 | various | Re-releases only; v2 |

Legacy chassis (Type-1…5, Zero, FM, Super-1, TZ, TZ-X, X) are catalogued as glossary entries only. Full table with wheelbase/tread/weight is in the concepts research; source: [Tamiya chassis select guide](https://www.tamiya.com/english/cms/mini4wd_chassis_select.html).

Key modelling fact: PRO chassis (MS/MA/ME) require double-shaft motors; all others require single-shaft. This is the first compatibility rule.

### 2.3 Motors

15 motors in two families (single-shaft and PRO double-shaft), each with RPM/torque/current ranges from Tamiya packaging and a legality set (Junior / Stock+B-MAX / Open / none). Full table in research; specs cross-checked against [MotorLab](https://motorlab-tw.github.io/en/benchmarks/tamiya-mini-4wd-motor-specs-list/). Japan-Cup coloured variants (95xxx) share specs with their 15xxx base motor.

### 2.4 Part taxonomy

- **Required to run:** kit (chassis + body + kit gears/wheels/tires/shafts/plastic rollers + usually FA-130 normal motor) + 2×AA batteries.
- **Optional (Grade-Up Parts):** rollers (plastic / aluminium / bearing; 8–19 mm), bearings, gear sets (3.5 / 3.7 / 4 / 4.2 / 5 : 1), counter and propeller shafts, wheels, tires, FRP/carbon plates and stays, brakes (stay + sponge grades), mass dampers, slide dampers, stabilisers, shafts, motor mounts, terminals, battery holders, body catches, screw/nut sets, tools, beginner bundles (First Try Parts Set per chassis, Starter Packs).
- **Item number scheme:** 18xxx regular kits, 19xxx Fully-Cowled/Aero kits, 15xxx regular GUP (GP No.), 95xxx limited/special (HG carbon, J-CUP motors, limited kits), 94xxx older limited. "HG" is a name prefix for premium items. "Hop-Up Options" is Tamiya's R/C line and does not apply.

### 2.5 Setup concepts for tutorials and the wizard

Gear ratio vs course type, roller width/placement (たからばこ setting), tire diameter and hardness, 90 g weight floor, brakes, mass dampers, front/mid/rear motor balance, flat vs 3D courses. Six beginner build archetypes will seed the wizard profiles: box run, first upgrade set, Stock/B-MAX bolt-on, flat speed, 3D stability, Open gimmick build (advanced, informational only).

---

## 3. Data sources and feasibility

| Source | Verdict | What we take |
|---|---|---|
| **Tamiya JP product catalog** `tamiya.com/japan/products/list.html?genre_item=30…` (1,203 Mini 4WD items; parts 631; regular GUP 244) | **Easy.** Server-rendered HTML, stable URLs, 20/page, no robots.txt. JP detail pages carry price, release date, description, 基本スペック, 使用可能シャーシ text and structured chassis tags. | Canonical item master |
| **Tamiya per-chassis compatibility pages** `product_info_ex.html?genre_item=mini4wd_chassis_{ar,ma,…}` (21 chassis codes; AR lists 428 items) | **Easy.** This is the machine-readable form of the GIF matching list. | Chassis ↔ part compatibility matrix |
| **Tamiya EN catalog** (351 items) | Easy but thin: names and one paragraph, no price/date/compat | English names |
| **tamiya.hk** (official HK distributor, WooCommerce) | **Easy.** Public Store API `/wp-json/wc/store/v1/products`, SKU = item number, HKD price, chassis filter taxonomy, TC category labels (導輪, 摩打, 避震器, 龍頭/鳳尾…). Names are English. | HK prices, TC taxonomy, HK terminology |
| **Tamiya PDFs** (lineup catalogs, parts catalog `parts2506_v3.pdf`, per-chassis parts lists MA/AR/FM-A 2021) | Medium. No per-kit assembly manuals are published online; PDFs are © Tamiya, no redistribution. | Reference for writing our own part-usage text |
| **Tamiya regulation, setting guide, chassis guide pages** | Easy (text) | Rule text for the rule engine and tutorials (paraphrased) |
| **Mini 4WD Fandom wiki** (608 articles; 91 GUP, 33 chassis, 337 cars; MediaWiki API works; CC-BY-SA) | Medium | Chassis history and car lineage, with attribution |
| **zh.wikipedia 迷你四驅** (CC-BY-SA) | Easy | Baseline TC glossary (馬達/摩打, 導輪, 軸承, 齒輪, 碳纖板) |
| Japanese community DBs (TEA-League, miniyonku.net, atwiki) | Hard / stale (CGI 500s, Cloudflare) | Occasional cross-check |
| 1999.co.jp, tamiyausa.com | Hard (bot detection) | Skip |
| Open datasets / GitHub | None exist | We build our own |

### 3.1 Initial data scope (≈300–400 items)

1. Chassis: MA, MS, ME, AR, FM-A, VZ, Super-II (+ VS as re-release).
2. Kits in production for those chassis (≈150–200).
3. All regular GUP (303010, 244 items) and AO parts (303030, 42).
4. Limited/special parts released since 2023-01 (≈120–180).
5. Skip in v1: legacy-era items, Wild/Dangun/Train, circuits, batteries beyond Neo Champ.

Neither Tamiya Stock Class nor B-MAX publishes an approved item list; both are rule-based. Legality is therefore a **derived per-item flag from category** (rollers, plates, dampers, brakes, bearings, motors are legal; tools and setting boards are not car parts) with manual overrides.

### 3.2 Pipeline

Built 2026-09-08 as `scripts/catalog/` — four stages, deliberately separated so a re-run is cheap and can never overwrite hand-authoring:

1. **Scrape wide** (`npm run scrape`, or `--only=jp,compat,hk,fandom`): JP list pages → item ids → JP detail → per-chassis compat pages → tamiya.hk Store API → Fandom wiki. Throttled to one request per 700 ms with a disk cache under `.cache/`, ≈930 requests per full run (the EN catalog turned out to be unnecessary — the JP detail page carries the English name). Output: committed snapshots in `data/raw/*.json` covering **every** item in the parts genres, not just the v1 selection, so widening the catalog later costs no requests.
2. **Generate narrow** (`npm run catalog:generate`): raw snapshots + `data/taxonomy/*.yml` + `data/overrides/parts.yml` → schema-validated YAML in `content/parts/` and `content/chassis/`. Everything under `content/` is machine-written and rebuilt from scratch on every run.
3. **Hand-authored layer**: `data/overrides/parts.yml` (keyed by item number) and `data/chassis/*.yml`. Category corrections, motor legality, slot graphs and TW naming live here, never in `content/`. `npm run catalog:report` lists what still needs a human.
4. **Cross-check** (`npm run catalog:crossref`): our derived categories against the Fandom wiki's hand-written `Parts type`, joined on item number. Our categories come from ordered substring rules over Japanese names, whose one failure mode is a short keyword swallowing a longer word — an independent taxonomy is the cheapest detector for exactly that. A QA source only: CC-BY-SA, and nothing from it is written into `content/`. Its first run joined 121 of our 382 parts and caught two live defects (`シール` matching `シールド` and `シールタイプ`; propeller shafts filed as axles) plus a missing propeller-shaft slot.

Attribution page: Tamiya (facts, links), tamiya.hk (prices, zh-HK names), Fandom/Wikipedia (CC-BY-SA text where reused).

First run committed **382 parts** (244 regular GUP + 42 AO + 96 limited/special/station released since 2023-01) and 8 chassis. Coverage at that point: 99% categorised, 83% with chassis compatibility, 80% with HKD prices, 60% with a Traditional Chinese name.

**Kits came in a second pass, into their own snapshot.** The parts run covered only parts genres, so `data/raw/tamiya-jp-items.json` holds 690 items whose ids begin with 10/15/55/84/94/95 and no 18xxx or 19xxx. `KIT_GENRE_SERIES` adds the 14 kit genres under `3010` and writes `data/raw/tamiya-jp-kits.json` (448 items), kept separate so a kit re-scrape leaves the parts diff untouched. See §4.7.

### 3.3 Images and IP

- Tamiya product photos are copyrighted. ~~Options: link out with a small hot-linked thumbnail and attribution (common practice, not formally licensed), or take own photos for the v1 scope.~~ **Settled 2026-09-16: self-hosted 160×120 thumbnails, no hotlinking** (§1 decision 4, §6 M1b). Own photos remain the standing plan for the ~50 parts that appear most in the builder and replace the thumbnails as they are taken. A clear "unofficial fan site" disclaimer stays, and the credit sits on the page that shows the pictures.
- **The Fandom wiki is not an alternative source for photos** (checked 2026-09-16). Its chassis images are Tamiya's own, uploaded under `{{Fairuse}}` with summaries saying "from Tamiya official website"; `ARChassis.jpg` carries no licence template at all. The wiki's CC-BY-SA covers its text, not these uploads, so taking a picture from there is taking it from Tamiya with an extra step — and at lower resolution (340×272). Tamiya's own chassis select page is the source we use.
- Item numbers and product names used nominatively in a catalogue are fine; avoid Tamiya logos, star mark and box art.

---

## 4. Technical architecture

### 4.1 Recommended stack

| Layer | Choice | Why |
|---|---|---|
| Framework | **Nuxt 4** (current 4.5) | Nuxt 3 reached end-of-life 2026-07-31; the scaffold's 3.14 is unsupported. Migration cost is near zero today (3 pages, 1 component). |
| Content | **@nuxt/content v3** with Zod-validated collections (parts, chassis, rules, guides) | SQL-backed collections, client-side WASM SQLite in static mode, D1 in serverless mode; Nuxt Studio is now free/OSS for editing. |
| i18n | **@nuxtjs/i18n v10**, `prefix_except_default`, **zh-Hant** default, `/en/…`, later `/ja/…` | Keeps existing root URLs, emits hreflang/canonical, avoids the static-generation quirk of pure `prefix`. Region-neutral rather than zh-TW because every Traditional Chinese name in the catalog came from tamiya.hk: a zh-TW label would serve Hong Kong wording under a Taiwan flag for 100% of parts and kits. Hong Kong and Taiwan differ by a glossary, not by pages, so they are a wording key over one page set (`app/composables/useWording.ts`), not two locales. ~~A reader-level HK/TW toggle~~ **withdrawn 2026-09-16** (§6 M1b): the catalog's names are all Hong Kong wording, so the toggle relabelled three glossary terms and left 382 part names untouched. `useWording` now returns `hk` as a constant; the `tw` data stays. Promote zh-TW to a routed locale only if content diverges beyond terminology. |
| 3D display | ~~**Google model-viewer** (already used) for hero, part previews, share pages~~ **The same plain-three scene component as the builder** (owner, 2026-09-16). model-viewer stays on the landing page only until the builder scene can draw a recognisable MA car, then it is retired (§5.5) | model-viewer is 256 KB gz from a third-party CDN; a three hero with GLTF + Draco loaders, room environment and orbit is 165 KB gz, self-hosted, and it is the component the builder needs anyway. What it gives up is AR (Scene Viewer / Quick Look), which nothing before M3 uses, and a poster-then-canvas paint the three component reproduces with a server-rendered `<img>`. |
| 3D builder | ~~**Three.js via @tresjs/nuxt** (v5.x, active)~~ **Plain three.js in one `.client.vue` component, no module** (owner, 2026-09-16, §5.5) on the builder route only, client-only and code-split | ~~Vue-native declarative scene graph; maps directly to the slot graph.~~ Measured 2026-09-16: TresJS costs 233–272 KB gz against 140 KB for a tree-shaken three scene, because `@tresjs/core` imports all of three as a runtime catalogue; `@tresjs/nuxt` also overwrites `vue.compilerOptions.isCustomElement` and installs `@nuxt/ui` for its devtools. The scene is one group per slot id and `getObjectByName(slotId).add(mesh)`, which a `watch` drives as well as a template would. Babylon is heavier; model-viewer cannot swap geometry. |
| Share/OG images | **nuxt-og-image v6** (Takumi renderer, runs on Workers) composing part list + a thumbnail captured from the builder canvas | Crawlers do not run WebGL, so the PNG must be pre-rendered. |
| Hosting | **Cloudflare Workers with static assets** (Pages is in maintenance mode), DNS for mini4wd.parts moved to Cloudflare | Unmetered static bandwidth, Worker only for `/api/*` and `/b/:id`. Free tier: 100K req/day, D1 5 GB / 5M reads / 100K writes per day (hard-enforced since 2026-09-01), R2 10 GB. |
| User data | **D1** (builds, votes, stats), **R2** (build thumbnails), **Turnstile** (abuse control) | Single vendor, no sleeping database (Supabase pauses after 7 idle days), no billing account needed (Firebase Storage now requires Blaze). |
| Auth | None in v1/v2 (device-id cookie + salted IP hash, one vote per build per device). v4: `nuxt-auth-utils` with GitHub/Google | Keep friction low; add identity only when garages/comments need it. |
| Analytics | Keep GA4; add PostHog for builder funnel, wizard completion, rule-warning frequency | Both live since 2026-09-18 through `@nuxt/scripts`, and ten events go to both from `app/composables/useAnalytics.ts` (§6 M0). Wizard completion waits on the wizard (M4). |
| Data tooling | Node scraper in repo; `@gltf-transform/cli` for model optimisation; ~~build123d (Python) for procedural part meshes~~ part shapes are generated in the browser by three.js from catalog specs, no Python toolchain and no per-part files (owner, 2026-09-16, §5.6) | See section 5. |

`@nuxthub/core` is optional sugar over the same Cloudflare bindings (its managed admin was sunset 2025-12-31; the module continues self-host-first). Using Nitro's `cloudflare_module` preset directly is equally fine and avoids a dependency.

MVP can stay on GitHub Pages: everything in M1 is static, the 3D view included — three runs in the browser. The move to Workers happens in M2 when short links and ratings need a database.

### 4.2 Data model

**parts** (one YAML per item, key = Tamiya item number)
- `id`, `names {ja, en, zh-TW, zh-HK?}`, `category`, `subcategory`, `series` (regular GUP / limited / special / AO / kit)
- `slots []` (which builder sockets it can occupy), `chassisCompat {include[], exclude[], notes}`
- `classLegality {open, stock_bmax, junior, notes}` (derived + override)
- `specs {rollerDiameterMm, rollerType, gearRatio, motorShaft, motorRpm[min,max], motorTorque, weightG, wheelDiameterMm, tireHardness, plateThicknessMm, …}`
- `priceJpy`, `priceHkd`, `releaseDate`, `status` (current / limited / discontinued)
- `images []`, `model3d {glb, attach}`, `requires []`, `tags []`, `roles []` and scoring attributes for the wizard

**chassis**: id, names, family, motor position, shaft type, wheelbase/tread/weight, kit gear ratios, `slots [{id, type, maxCount, mirror, required}]` (plus `socketNode` when the 3D sockets land, §5.2), kits [].

**rules**: declarative JSON (see 4.3). **guides**: Markdown per locale. **profiles**: wizard weight vectors.

**D1 tables**: `builds(id, short_id, chassis_id, parts_json, class, title, locale, thumb_key, created_at, view_count)`, `votes(build_id, voter_hash, value, UNIQUE)`, `part_votes(part_id, voter_hash, value)`, `build_stats` (materialised).

A compact `catalog.json` (ids, names, category, slots, compat, key specs) is shipped to the client so the builder and rule engine run without database reads.

### 4.3 Rule engine

Declarative rules evaluated in the browser and re-validated by the Worker at share time. Rule types: slot capacity, unfilled `required` slots (§4.2 — the build is not a runnable car), chassis compatibility, motor shaft type, class legality (per selected class), dependencies (`requires`, e.g. bearing rollers need 2 mm screws), mutual exclusion, numeric constraints (total width ≤ 105 mm, tire 22–35 mm, weight ≥ 90 g estimate). Output is a list of `{ruleId, severity: error|warning|note, parts[], message{locale}}` displayed inline, PCPartPicker style. Never claim 100% coverage; show "verify against organiser PDF" notes for class rules.

### 4.4 Wizard / recommender

A rules-based scorer over the same catalog. Inputs: chassis owned or none, class, course type (flat / 3D / unknown), budget, experience. A `profiles` collection maps answers to weight vectors over part attributes (stability, speed, cost, ease); hard filters are compat, legality and budget. Output is a build the user can open in the builder. Community popularity (votes, inclusion counts) is added as one more weight once M2 has collected it — which is why the wizard sits in M4, after the data that makes it good. No ML.

### 4.5 Sharing

- M1: build encoded in the URL (versioned compact byte array → base64url in the hash of `/`, since 2026-09-16 — §6 M1b has the layout). Zero backend; share page renders the part list and a ~~model-viewer~~ three preview of the chassis (§4.1, 2026-09-16).
- M2: `POST /api/builds` → short id, `/b/:id` page with OG image (Takumi card with part list + R2 thumbnail). Fallback OG card per chassis when no thumbnail exists.
- M3: `/b/:id` embeds the interactive 3D assembled car.

### 4.6 i18n and SEO

One prerendered page per part (`/parts/<item number>`), chassis, guide and class, in each locale; JSON-LD `Product` and `BreadcrumbList`; ~~per-locale sitemaps~~ **one `/sitemap.xml`** (2026-09-16): both locales are in it, each URL carrying the other as an `xhtml:link` alternate, which is the same information in one request rather than three. Part names in all locales live inline in the YAML so a single record feeds every locale; guide bodies are separate Markdown files per locale, one @nuxt/content collection each (`content/zh-Hant/**`, `content/en/**`).

**URLs are flat; the hierarchy is declared, not routed** (owner, 2026-09-16, §6 M1b). A part lives at `/parts/<item number>` and a chassis at `/chassis/<id>`, one segment deep, and the browse levels above them are their own routes — `/parts`, `/parts/category/<slug>`, `/chassis`. Nesting the detail pages under their category was rejected for three reasons that all still hold: GitHub Pages cannot 301, so moving a live URL breaks every share link already in the wild; a part has one category but several slots and up to eight compatible chassis, so a nested path picks one parent arbitrarily; and `BreadcrumbList` is JSON-LD, so the trail a reader and a crawler see is free of the path either way. A category slug stays English in every locale — only the `/en` prefix changes — because a translated dynamic segment needs a per-locale route table and percent-encodes badly when pasted.

**Every URL ends in a slash** (owner, 2026-09-17). Pages prerender to `<route>/index.html`, and GitHub Pages answers `/parts/15512` with a 301 to `http://…/parts/15512/` and a second 301 back to https, so the bare form that every canonical, hreflang, og:url, breadcrumb and sitemap URL used to name was a redirect chain, not a page. The slash form is also the one the redirect has been handing out and search engines have indexed, so it was chosen over writing `parts/15512.html`, which would keep the bare URLs and 404 the slash ones. Bare links keep working through Pages' redirect. Three settings in `nuxt.config.ts` have to agree — `site.trailingSlash` (sitemap), `i18n.trailingSlash` (canonical, alternates, `localePath`) and the `nuxtLink` default (every internal link) — and the prose catch-all declares its own route pattern, because `[...slug]` reads `/about/` as `['about', '']` and i18n rebuilt that as `/about//`.

Breadcrumbs are also the site's only **upward** links. The internal graph points down from the builder into parts and sideways between variants and neighbours; without a trail, every catalog page hangs off one payload-heavy home page.

Traditional Chinese is one page set, not two: `zh-Hant` at the root, with `zh-HK` and `zh-TW` hreflang alternates pointing at the same URL. ~~A wording toggle decides which regional term a reader sees~~ — withdrawn 2026-09-16 (§4.1); the page reads in Hong Kong wording, and zh-TW hreflang stays because the page is readable in Taiwan. `resolveName` in `shared/catalog/names.ts` still takes a wording argument and prose still goes through the `:term{name="…"}` MDC component, so the seam is kept.

### 4.7 Kits and build presets

The MVP's two entry points — bare chassis and existing kit — are the same object with different seeds.

~~**A build** is `{ chassis, kit?, class, slots: { [slotId]: Array<{ partId, origin }> } }`, where `origin` is `stock` or `swapped`.~~ **Corrected 2026-09-09 against what shipped.** A build is stored as a delta and nothing else: `BuildState = { chassis, kit?, swaps }` (`shared/catalog/build.ts`). `origin` is *computed* by `resolveBuild`, not stored, and it has **three** values, not two — `chassis`, `kit`, `user`. Keeping the kit as a middle layer rather than expanding it into `swaps` is what makes three things fall out for free: a "what you changed from the box" diff, a shopping list of only the parts you still need to buy, and a legality check that can say *which* modification broke Stock Class.

**Both entry points are one state, and `kit` is optional rather than a union** (evaluated 2026-09-09). `newBuild(chassis, kit?)` and `resolveBuild(chassis, kit | undefined, state)` treat the bare-chassis case as the same object with one layer skipped, so nothing downstream — rule engine, totals, URL codec, 3D socket graph — branches on which door the user came through. A discriminated `KitBuild | ChassisBuild` would push that branch into every consumer for no gain. Neither door can be dropped: kits cover only what Tamiya still lists (ME has **2** kits against MA's 69), while the 8 chassis cover 100% of v1 scope and match how racers actually buy a bare chassis plus a body.

**Flattening a kit into a resolved parts list was considered and rejected** (2026-09-09). The measurement settles it: of 1,783 kit loadout entries, **0 carry an item number** — a kit specifies 6 slots (277 kits), 7 (15) or 1–2 (13) against a chassis of 19–22, because the wiki's `Technical Info List` records what *distinguishes* a box, not what is in it. Flattening would write ~2,200 entries asserting things no source states, sitting indistinguishably beside the wiki-sourced ones, and would cost ~2.2× the loadout payload to say nothing new. It would also destroy `origin` (every slot would read `user`), break the shopping list (no longer an M1 criterion since 2026-09-16, §6 M1b), bloat the URL encoding, and strand the CC-BY-SA `loadoutSourceTitle` attribution. The flat list the UI wants already exists — it is `resolveBuild`'s `ResolvedSlot[]`. Delta is the storage form; the flat list is the read form.

- **Bare chassis:** seed from the chassis' `defaultLoadout` (kit gears, plastic rollers, kit wheels/tires, shafts, FA-130), leave every slot it does not name empty. `defaultLoadout` decides what is seeded, not the slot's `required` flag (§4.3).
- **From a kit:** seed from the kit's `stockLoadout` — the chassis `defaultLoadout` overlaid with the kit's own body and its per-kit differences, since a kit may ship a different gear ratio, low-profile wheels or harder tires.

#### Where the kit data comes from

Two sources, and the split matters because they have opposite gaps.

**Tamiya** has no kit records in our snapshot at all (§3.2), but remains the only source for **price, release date, official URL and the zh-HK names** the HK store join supplies — so the kit genres have to be added and re-scraped regardless.

**The Fandom wiki turns the loadout from hand-authoring into an import.** Tamiya publishes no structured bill-of-materials — its kit pages give the chassis and prose — but the wiki's `Template:Technical Info List` is a per-item spec table, and it is well populated. Measured 2026-09-08 across 305 articles:

| | |
|---|---|
| rows (one per kit variant) | 819 |
| distinct item numbers | 819, of which **334 are 18xxx/19xxx kits** |
| chassis type | 99.9% |
| wheel size / type / spoke / fitment / colour / material | 99–100% |
| tire size / type / colour / material | 99.6–100% |
| gear ratio | 98.2% |
| motor | 86.8% |
| body colour / material, L × W × H | 94–100% |

That is exactly the **kit-specific delta** — the slots where kits differ from one another. What it omits (plastic rollers, shafts, bearings, terminal, gear cover) is precisely what the chassis `defaultLoadout` supplies uniformly. So the two sources compose: wiki for the delta, `defaultLoadout` for the rest, `data/overrides/kits.yml` only for the outliers rather than for all 300-odd kits by hand.

Caveats to hold onto: the wiki skews toward notable and anime-derived cars over current shelf stock, so a recent kit may be missing; its fields are free text (`Wheel type=Low-Profile Fin-Type`); and it is **CC-BY-SA**, so kit pages that use it must attribute, not just the attribution page. Tamiya stays canonical wherever the two disagree on a fact.

#### What the join actually yields (built and measured 2026-09-08)

The 819 rows are not 819 usable kits — over half sit on chassis we do not model. Walking all 14 kit genre list pages and joining on item number gave:

| | |
|---|---|
| Tamiya kit ids across the 14 in-scope genres | 448 |
| of those, carrying a chassis tag | 433 |
| **of those, on a v1 chassis — the committed catalog** | **305** |
| by chassis | ma 69, vs 50, super-2 50, ms 50, ar 34, fm-a 29, vz 21, me 2 |
| covered by a wiki loadout | 96% (13 kits fall back to the chassis default) |
| with a gear ratio | 97% |
| with an HKD price | 78% |
| with a Traditional Chinese name | 33% |

**Deciding a kit's chassis needs both signals, in the right order.** The chassis tags on a kit page are a *compatibility* list, not a statement about the box: ロボレース デボット2.0 (MAシャーシ) is tagged `ms`, `ar` and `ma`, so reading the first tag files an MA kit under MS — a wrong slot profile and a wrong `defaultLoadout`, and nothing downstream can catch it because MS is a real chassis. So the **name** wins where it names a chassis, the **tag** is trusted only when it leaves one in-scope answer, and the rest are left for `data/overrides/kits.yml`. Across 448 kits that is 4 with multiple tags, of which 2 the name settles, 1 is out of scope anyway, and exactly 1 (95586 ディオマース・ネロ) needs a human.

Three findings changed the design, and all three point the same way — **the wiki describes a box, it does not enumerate purchasable parts**:

1. **The motor field is worthless and does not need to be.** 423 of 463 v1 rows say `Standard`, which is the normal motor the chassis `defaultLoadout` already supplies. Only a real upgrade is a delta worth storing.
2. **Tamiya rarely supplies the gear ratio.** Its 【基本スペック】 block carries `ギヤ比` on about 5% of kit pages (2 of a 40-kit sample), against the wiki's 98%. So the two are not interchangeable: **Tamiya is canonical for identity, price, date and chassis; the wiki carries the loadout.** Where Tamiya does print a ratio it still wins, and those are the newest kits — which is how ME gets one despite having no wiki row.
3. **Most loadout entries can never have an item number.** `Wheel type = Low-Profile Saber-Type` is moulded into the kit and Tamiya has never sold it as a Grade-Up Part. So a loadout entry carries an optional `partId` **and** a free-text `label`, and a label with no item number is the normal case. That is also what makes the shopping list correct: an entry with no `partId` is something you already own and cannot buy.

That last point retires the "free-text → item-number lookup table per chassis" this section used to call for. There are 47 distinct wheel types and 14 tire types across the v1 rows, and authoring a mapping for them up front would mostly produce mappings to nothing. `npm run catalog:report` lists the labels by frequency instead, so `data/overrides/kits.yml` can name the few that really do correspond to a catalog part.

ME is the coverage hole — Tamiya lists only two ME kits so far and the wiki covers neither — but that is mitigated: Tamiya prints `ギヤ比` in its own spec block on about one kit page in twenty, and those are the newest kits, so ME still gets a real gear ratio. Where the two disagree Tamiya wins, per §3.

#### Schema and work

**New `kits` collection** (`content/kits/*.yml`, generated like parts): `id`, `names {ja, en, zh-TW, zh-HK}`, `series`, `seriesNumber`, `chassis`, `gearRatio`, `stockLoadout`, `loadoutSource`, `loadoutSourceTitle`, `priceJpy`, `priceHkd`, `releaseDate`, `status`, `officialUrl`. `bodyArchetype` is deliberately absent until the 3D work needs it: nothing can fill it yet, and the catalog omits fields it cannot fill rather than stubbing them.

**New field on chassis:** `defaultLoadout` — slot id → what the bare runner provides. Hand-authored in `data/chassis/*.yml`, eight records.

A kit stores only the **delta** over its chassis' `defaultLoadout`, not the merged result. Seeding a build merges the two, which the bare-chassis entry point needs anyway, and it means re-authoring a chassis default does not require regenerating every kit that uses it.

1. Add `KIT_GENRE_SERIES` to `scripts/catalog/sources/tamiya-jp.ts` alongside `GENRE_SERIES` (14 codes; Wild, Dangun and Train stay out per §3.1). The genre becomes a type parameter on `ListEntry`/`JpItem`, so a kit reaching the part builder is a compile error rather than a bogus `gupNumber`.
2. Add a `Technical Info List` reader to `sources/fandom.ts`. `parseTemplate` had to become `parseTemplates`: a car article carries one call per variant and the old parser returned only the first.
3. Scrape kits into their own `data/raw/tamiya-jp-kits.json`, so a kit re-scrape leaves the parts diff alone.
4. Author `defaultLoadout` for the eight chassis.
5. Extend `shared/catalog/schema.ts` with `kitSchema`, `loadout` and `defaultLoadout`, register the collection in `content.config.ts`, and extend `catalog:verify`. The verify check has to resolve a loadout's slot **id** to the chassis profile's slot **type** before testing a part against it — the profile calls a shaft slot `axle` and a gear slot `gear-set`, so comparing ids to types directly would pass almost anything.

Scoping note: M1 does not need all 448 kits, only enough current, in-shops kits that a beginner recognises the box on their desk. The 292 that landed with a wiki loadout make that a coverage question rather than an authoring one.

Two prefix fixes fell out of this work: the wiki item-number pattern was missing `17`, `92` and `93` (limited kit variants), which is why the count above is 819 rather than the 639 first measured, and the tamiya.hk SKU filter was missing `17` (Beginner's kits).

### 4.8 Localising loadout labels

Surfaced by the builder spine on 2026-09-09 and scoped the same day. `loadoutEntry.label` is a single untranslated string, so the chassis layer (hand-authored Traditional Chinese) renders Chinese on `/en`, and the kit layer (English imported from the wiki) will render English on `/zh-Hant` the moment the kit entry point lands. **The bug doubles rather than holding steady, which is why this chunk goes first.**

Scoping it turned up the thing that makes it cheap: **`label` is doing four unrelated jobs**, and only one of them is a translation problem. Measured across the committed catalog:

| Job | Entries | Distinct | What it actually is |
|---|---|---|---|
| Body | 305 | 305 | **Byte-identical to `kit.names.ja` in 305 of 305 cases** — the generator writes `item.nameJa` into both. The kit already carries `en` and `zh-HK`. |
| Gear set | 295 | 8 | **Byte-identical to `kit.gearRatio` in 295 of 295 cases.** A ratio (`3.5:1`) is locale-neutral. |
| Motor | 15 | 6 | Real Tamiya products — Atomic-Tuned 2, Rev-Tuned 2 (PRO), Torque-Tuned 2 (PRO), Light-Dash. **These have item numbers in our catalog.** |
| Wheels and tires | 1,168 | 62 | Descriptive phrases the wiki composed from `size + type`. The only genuine translation surface. |
| *(chassis layer)* | 88 | 11 | Hand-authored `套件標準…` strings in `data/chassis/*.yml`. |

So **615 of 1,783 kit entries need no translation at all** — they are duplicates of fields that already exist one level up in the same record, and the fix is to stop restating them. What remains is ~62 phrases, and dirtier than the count suggests: the wiki spells the same wheel `Fin-Type` and `Fin-type`, `Manta Ray Type` and `Manta Ray-Type`, `Fully Cowled Type` / `Fully Cowled-Type` / `Fully Cowled-type` / `Fully-Cowled Type`, once truncates `Saber-Type` to `Saber-Typ`, and inverts word order in `X Narrow-Type` / `X-Type Narrow`. Normalising first collapses 62 to roughly 40. Four rows are compound free text (`Small (Both) / Low-Profile Fin-Type (1st) / Low-Profile Dish-Type (2nd)`) and stay untranslated rather than being parsed.

#### The schema change

`label: z.string()` becomes a **partial** name record — the same four keys as `partNames`, but every one optional, because a wiki phrase has only `en` and a hand-authored chassis label has only Traditional Chinese. `loadoutEntry`'s existing `partId || label` refinement still holds; the label itself gains a "at least one locale" refinement so an empty record cannot validate.

`resolveName` (`shared/catalog/names.ts`) keeps its current total contract for `Names`, where `ja` is required and the chain always terminates. A partial-tolerant sibling returns `undefined` when nothing matches, and `useCatalogName` exposes it. Untranslated phrases then show in whatever locale we have, marked by the **existing** `isFallback` treatment — the same one already used for the 40% of parts and 67% of kits whose best name is Japanese. This is not a new pattern, it is the established one applied to a fourth field.

#### What each job becomes

- **Body** — the generator copies the kit's own `names` record into the label instead of re-writing `nameJa`. 305 entries localise for free, in three locales, with no translation authored.
- **Gear set** — the ratio written into every locale key we serve. Three copies of a five-character string is the dumbest thing that works; the alternative (a `neutral` entry kind, or storing it only under `ja` and relying on both fallback chains ending there) buys nothing except a false `isFallback` flag to suppress.
- **Motor** — not a label problem. All six resolve to catalog parts through `data/overrides/kits.yml`, which is exactly what that file exists for; `catalog:verify` already rejects a `partId` that is not in the catalog or does not fit the slot, so a wrong guess fails the build. They then localise through the part's own `names` **and become linkable in the builder**, which a label never could be.
- **Wheels and tires** — a shared vocabulary keyed by the normalised English phrase, in a new `data/taxonomy/loadout-labels.yml`. Keyed by phrase rather than by item number because the same wheel appears across dozens of kits; `data/overrides/kits.yml` is the wrong shape for it and stays the place for per-kit corrections.
- **Chassis layer** — 11 strings, hand-authored English added beside the existing Chinese. Deriving them from the `build.slot.*` message keys (which are already translated in both locales) was considered and rejected: three of the eleven carry information the slot name does not — `雙軸` (double-shaft, PRO chassis only), `塑膠軸襯` (the stock part is a plastic **bushing**, while the slot is `bearing`), and `塑膠導輪` (plastic roller) — so the machinery would exist to save eight translations and would quietly drop the three that matter. While editing these, split the wording: `套件標準雙軸馬達` uses the Taiwan term in an otherwise Hong Kong-default dataset, and the record now has separate `zh-HK` / `zh-TW` keys to say so.

#### Work, in order

1. `shared/catalog/schema.ts` — `label` to a partial name record; refine for non-empty.
2. `shared/catalog/names.ts` — partial-tolerant resolver; `app/composables/useCatalogName.ts` exposes it.
3. `scripts/catalog/generate.ts` — body copies `names`; gear ratio written per locale; normalise wiki wheel/tire phrases **before** they are used as lookup keys, or the same string gets translated twice.
4. `data/taxonomy/loadout-labels.yml` — new, ~40 entries. `npm run catalog:report` already ranks unresolved labels by frequency and supplies the worklist; re-key it to the normalised phrase.
5. `data/overrides/kits.yml` — the six motors to real `partId`s.
6. `data/chassis/*.yml` — 11 labels gain `en`, and the motor label splits HK/TW.
7. `shared/catalog/build.ts` (`fromLoadout` passes the record through), `app/components/build/SlotRow.vue` (`entryText` resolves it), and the five label assertions in `shared/catalog/build.test.ts`.

Blast radius is seven files plus the two data directories; every consumer of `entry.label` is listed above.

**Two costs accepted rather than fixed** (review, 2026-09-09). A kit's body label is a verbatim copy of the kit's own `names`, 36 KB across 305 records; it buys uniformity — every slot resolves the same way, with no branch asking "is this the body?" — and it ships nothing today, because `/build` does not load kits until the next chunk. Revisit it there, where the payload cost is real and measurable rather than hypothetical. A gear ratio is stored under all four locale keys, 26 KB, so that a string needing no translation is not italicised as an untranslated one; it is repeated text and compresses to near nothing.

**Revisited and closed 2026-09-09, when the kit entry point shipped: keep the body label, and the margin is wider than 36 KB suggested.** The payload interns each distinct string once, and a body label is byte-identical to the kit's own name in 305 of 305 cases, so the duplicate resolves to the *same* table entry and costs a handful of index references rather than a second copy of the text. Removing it would buy almost nothing and would put back the "is this the body?" branch it paid to avoid.

#### Rendering the third origin

Landing with the same chunk, because it is the other half of the same problem and cannot be seen until the kit door opens. `resolveBuild` computes `origin: 'chassis' | 'kit' | 'user'` on every entry it returns and **no component reads it** — `SlotRow.vue` collapses it to a binary stock/swapped badge, and the CSS class is already called `.slot-origin`. Three states in the data, two on screen, and the discarded one is exactly the distinction a beginner needs: *what came in my box* versus *what we inferred from the chassis*. Render all three — "from your kit", "standard for this chassis", "you chose this". That makes the chassis-default inference visible and correctable rather than presented as sourced fact, and it is what makes a shopping list of what you still need to buy computable (dropped from the M1 criterion 2026-09-16, §6 M1b). One prop and three message keys.

---

## 5. 3D builder feasibility

### 5.1 Findings

- **No reusable part-separated model library exists.** Sketchfab has one CC-BY Super-1 chassis (769k triangles, legacy chassis) and a few full cars of unclear licence; print sites hold custom accessories, not Tamiya GUP shapes. Everything found is dimensional reference only. The old `public/images/4wd.glb` was a single baked model (3 meshes, 66k triangles), usable as a hero asset but not as a builder base. It was deleted with the model-viewer landing page (2026-09-16).
- **Image-to-3D is not the main pipeline.** TRELLIS.2 (MIT), Hunyuan3D 2.1, Tripo and Meshy produce acceptable stylised **bodies** from a 3/4 product photo, but soften edges and lose sub-millimetre features (roller flanges, plate holes, gear teeth). Tamiya's 1–3 product photos are not the orthographic multi-view these models want. Photogrammetry of a real chassis works; black FRP and chrome rollers do not.
- **Procedural CAD is the sweet spot** for ~70% of parts: rollers, wheels, tires, plates, mass dampers, spacers and screws are revolves and extrusions with dimensions already in the catalog. build123d or CadQuery export GLB directly; one script with parameter tables regenerates the whole library in CI.
- **Blender** for chassis (2 in MVP, with named socket empties) and bodies (stylised, low-poly, no decals, for IP reasons).
- **Licensing:** Tamiya holds patents, design rights and trademarks. Simplified, dimensionally plausible representations referencing generic categories are the defensible path; do not offer GLBs for download; add a disclaimer.

### 5.2 Architecture

- One GLB per part variant, shared unit system, origin at mounting point, 6–8 shared PBR materials so parts can be recoloured at runtime.
- ~~Chassis GLB carries named empty nodes as sockets (`socket_front_stay`, `socket_roller_FL_upper`, `socket_wheel_FR`, `socket_body`, `socket_motor`).~~ **Retired 2026-09-16 (§5.6):** there is no chassis GLB. Each chassis is a generator in `shared/scene/generators/chassis.ts` and its sockets are the table in `shared/scene/sockets.ts`, named by slot id, in the same coordinate system as the geometry. Plates carry their own sockets so the part tree cascades (chassis → plate → roller). A JSON slot graph (slot → allowed categories, default, mirror flag) is the same structure the 2D builder and rule engine use.
- Pipeline: Blender / ~~build123d~~ → `gltf-transform optimize` (meshopt, quantisation, KTX2 for body textures) → `public/models/<category>/<id>.glb`. ~~**Amended 2026-09-16 (§5.6):** this pipeline is for the chassis only. Parts and bodies are three.js geometry built in the browser from the record's specs; no GLB is written for them.~~ **Retired later the same day (§5.6):** the chassis went the same way, so nothing on the site is a GLB and no loader ships. This bullet stays as the record of what was considered; `gltf-transform`, meshopt and KTX2 return only if a baked export is ever wanted for share pages (M3).
- Runtime: `InstancedMesh` for rollers/screws; invisible oversized proxy volumes at sockets for raycast click-to-select (so 13 mm rollers are tappable on mobile); emissive highlight on hover; ghost placeholders for empty slots; preload chassis + defaults, lazy-load variants.
- Nuxt pitfalls: never import three at module scope in SSR paths; loaders in `.client.ts`; decoder WASM served from `public/`.

### 5.3 Effort

| Asset class | Count | Method | Hours |
|---|---|---|---|
| Chassis (MA + one of MS/VZ) with sockets | 2 | Blender | 30–40 |
| Bodies (stylised) | 4 | AI blockout → Blender retopo | 30–40 |
| Plates / stays / brake plates | 8 | Procedural | 15–20 |
| Rollers, wheels, tires, small parts | ~21 | Procedural | 12 + framework |
| Bumpers / dampers | 4 | Blender | 12–16 |
| Motor, gears, battery, switch | 5 | Blender low-poly | 5–10 |
| Materials, procedural framework, socket QA | | | 30–40 |
| **Total** | **~65–75 meshes** | | **~150–190 h** |

Roughly 4–6 weeks part-time for a one-person MVP asset set. Scaling to hundreds of GUP items afterwards is mostly parameter tables, which is the main argument for the procedural route.

### 5.4 Cutting that down for M1

150–190 h is the roadmap's long pole and it is not an MVP. Three decisions cut it to roughly **40–50 h** without giving up the thing the owner actually asked for — watching a 3D car change as you swap parts.

**1. Parametric by category, never per item.** A 19 mm aluminium roller and a 19 mm plastic roller are one revolve with two parameter sets. Build the generators against `category` + `specs`, not against item numbers, and 383 parts collapse to about a dozen scripts. The catalog already carries the parameters (`rollerDiameterMm`, `wheelDiameterMm`, `plateThicknessMm`, …), which is exactly what §3's spec extraction was for.

**2. A universal fallback, so no build can fail to render.** Any part with no generator draws a labelled grey proxy sized from its specs (or a category-default box). This is the load-bearing decision: it decouples asset production from catalog coverage entirely. The builder ships when the *chassis* is done, and every generator added after that is a silent visual upgrade with no code change. Without this rule, 3D blocks on 383 meshes and never ships.

**3. One chassis, ~~generic bodies~~ per-kit silhouettes.** §7 already mitigates the asset risk with "start with one chassis (MA) and ~20 parts to validate UX". ~~M1 takes that literally: **MA only** in 3D.~~ MA is the right pick to start — most kits (69), monocoque so it is one mesh, and beginner-facing. ~~The other seven chassis get the full 2D builder plus a still image and a "3D preview coming" note.~~ **Amended 2026-09-16 (owner, §5.6):** MA is first, not only. Once MA proved the primitive helper was enough, the other seven were pulled into M1: every build sits on a chassis, 236 of 305 kits sit on one of the seven, and a chassis from primitives is 2–4 h, so the still-and-notice fallback was costing more readers than it saved hours. ~~Bodies are **3 generic stylised archetypes** assigned per kit, not per-kit shapes: bodies are simultaneously the most expensive geometry and the highest IP risk (§5.1), so buying only 3 of them is doubly right. Label them in the UI as representative shapes.~~ **Amended 2026-09-16 (owner, §5.6):** a shell should briefly resemble the car on *that* kit's box art — not accurately, but recognisably. So bodies are per-kit **silhouettes**, each a dozen numbers in a shared shape table that one three.js loft draws, and kits that share a body (recolours, chassis re-releases) share the entry. Still labelled as representative shapes.

**Sockets reuse the slot ids that already exist.** ~~The empty nodes in the chassis GLB~~ The socket groups the pane builds from `shared/scene/sockets.ts` (a GLB until 2026-09-16, §5.6) are named for the ids in `data/taxonomy/slots.yml` — `roller-front`, `wheel-rear`, `front-stay`, `body`, `motor`. No socket-name mapping table, no second data model: the 3D pane reads the same build object the 2D list writes, and `parent.getObjectByName(slotId).add(mesh)` is the whole attach step. Mirrored slots (`mirror: true`) get `-l`/`-r` suffixed sockets and one mesh instanced twice.

**Priority is what you can see.** Body, wheels, tires, rollers, stays and chassis colour carry essentially all the visual difference between two builds. Motor, gears, terminal, bearings, switch and screws are hidden or sub-millimetre — crude shapes or nothing, and no one will notice.

| M1 asset | Count | Method | Hours |
|---|---|---|---|
| ~~MA chassis, slot-named socket empties~~ All eight chassis, each a generator over the shared box/prism/revolve helper plus its socket table (§5.6) | ~~1~~ 8 | ~~Blender~~ three.js in the browser | ~~15–20~~ 2–4 each, 20–30 in all |
| ~~Generic stylised bodies~~ Body silhouettes, one loft plus a shape table authored from box art (§5.6) | ~~3~~ 1 loft, MA's 69 kits' shapes first | ~~AI blockout → Blender retopo~~ three.js in the browser | 8–12 for the loft and the MA table |
| Parametric generators (roller, wheel, tire, plate/stay, brake, mass damper, spacer, screw, bearing, shaft, motor, gear) | ~12 | ~~build123d → GLB~~ three.js in the browser (§5.6) | 10–14 |
| Shared materials, fallback proxy, socket QA | — | | 6–8 |
| **M1 total** | | | **~40–50 h** |

**M1's 3D pane is the selection surface.** ~~Selection stays in the 2D list: it is faster, it works on a phone, it is accessible, and it needs no raycast proxies, no hover outlines and no touch tuning. Click-the-car-to-select is deferred to M3 (§6) with the rest of §5.2's interaction work.~~ Reversed 2026-09-09 (§1, guiding decision 1). Clicking the car brings §5.2's raycast proxies, hover highlight and touch tuning into M1 — the oversized invisible proxy volumes are what make a 13 mm roller tappable on a phone, so they are not optional once selection moves into the canvas. Budget roughly 15–25 h on top of the ~40–50 h above.

**3D is primary and the first thing seen; the list is the accessibility, efficiency and backup path** (owner, 2026-09-09). Those are three distinct jobs, and none of them is a stopgap:

- *Accessibility.* Keyboard reachable and screen-reader legible. A canvas is not, and no amount of raycast tuning makes it so.
- *Efficiency.* A reader holding the box and reading "15549" off it wants a search field, not a camera orbit. The fast path for someone who already knows what they want is a list, and that stays true after 3D covers everything.
- *Backup.* Some readers will simply not want to pick parts by clicking a 3D car, for whatever reason, and are owed the traditional list — the deliberately unexciting PC-parts-picker table, the kind of UI someone specs a desktop build with. That is a **preference**, not a limitation to engineer away: no amount of raycast polish retires it, because it was never about the 3D working badly.

**None of the three expires.** The list is not scaffolding to be removed when the meshes land; it is a permanent second surface, and the only open question is which one opens by default.

Separately and temporarily, the list is also the *only* surface for most of M1: one chassis of eight is modelled, and a part with no generator draws a labelled proxy rather than a recognisable shape, so a reader on VZ — or on a slot that is all grey boxes — has nothing meaningful to click. That condition does expire. It is what protects the M1 date (§6 M1c); it is not what the list is for, and conflating the two is what makes the list look temporary.

Because the choice is a preference, it is remembered per reader rather than reset each visit — the same shape the HK/TW wording toggle had before it was withdrawn (§4.1): a reader-level preference in `localStorage`, restored after hydration. Someone who switches to the list once should not be handed the canvas again tomorrow.

### 5.5 M1c spike: the pane before the meshes (scoped 2026-09-16)

M1c is the long pole (§6), and nothing in 3D has been started. The part of it that can slip is not Blender: it is the three things nobody has tried in this repo yet — three.js inside a prerendered Nuxt route, the size of a code-split 3D chunk on a page that already ships ~118 KB gzipped of JavaScript, and whether a 13 mm roller can be tapped on a phone. §5.4's fallback proxy means all three can be answered with **zero meshes**: a socket group per slot id, a box in each, a raycast to pick one. So the first M1c chunk is a spike, not the chassis model. `/build` grows a canvas above the slot list; the canvas draws MA as labelled proxy boxes at hand-placed sockets; tapping a box opens the same `BuildPartPicker` the list row opens; the box changes when the build does. Nothing else.

#### Measured before starting (2026-09-16)

| Bundle (esbuild, minified, gzip -9, `vue` external) | KB gz |
|---|---|
| `/build` today, all JS the prerendered page modulepreloads | ~118 |
| three 0.186.0, tree-shaken to a minimal scene (renderer, camera, box, standard material, raycaster, lights) + `OrbitControls` | 140 |
| `@tresjs/core` 5.9.0, `TresCanvas` alone | 233 |
| `@tresjs/core` + `@tresjs/cientos` `OrbitControls` | 272 |
| Whole `three` namespace, nothing shaken (the ceiling) | 191 |

The TresJS numbers are not a packaging accident. `@tresjs/core` imports the whole of three as a namespace and registers it as the catalogue that resolves `<TresMesh>`, `<TresBoxGeometry>` and the rest by name at runtime, so a bundler has nothing it can prove unused. That is the design, and it means TresJS costs the full library plus its own ~40 KB, roughly **twice** a hand-written scene of the size this pane needs.

Two more things, found reading `@tresjs/nuxt` 5.7.0's module source rather than its docs:

- It **assigns** `nuxt.options.vue.compilerOptions.isCustomElement` rather than composing with what is there. `nuxt.config.ts` sets that option so `<model-viewer>` renders during SSR on the landing page; the module would replace it, and the hero would fall back to the `<client-only>` path the config comment explains was rejected for a 0.1 layout shift. Fixable with a `modules:done` hook that re-wraps the function — but it is a trap that fires on the first install, on a page nobody would think to re-check.
- It depends on `@nuxt/ui` ^4.8 (Tailwind and Reka behind it) for a prebuilt 3.4 MB devtools panel. None of it reaches the app bundle; it is a large install for a devtools tab.

**Decided: plain three in one `.client.vue` component, no module** (owner, 2026-09-16; §4.1 amended). The case: half the bytes; nothing to unpick in `nuxt.config.ts`; and the scene here is not declarative in any useful sense — it is one group per slot id and `getObjectByName(slotId).add(mesh)`, which §5.4 already calls "the whole attach step". Vue still drives it: a `watch` on the resolved slots re-populates the socket groups, the same way the list re-renders. What is given up is the TresJS devtools tab and `<TresMesh>` template syntax, neither of which M1c is short of. Step 1 below records what the chunk actually costs once Nuxt has split it.

**The same component becomes the hero, and model-viewer goes** (owner, 2026-09-16). Asked whether model-viewer is still needed: not once the builder scene exists. Measured the same day, model-viewer 4.0.0 is 256 KB gz from Google's CDN, against 165 KB gz for three with the GLTF and Draco loaders, a room environment and orbit controls — self-hosted under `/_nuxt/`, which also drops the two third-party hops the landing page preconnects to. `public/images/4wd.glb` is Draco-compressed, so both stacks fetch the same ~190 KB decoder. What model-viewer has that three does not: AR through Scene Viewer / Quick Look, which nothing before M3 uses and which needs a baked export anyway; a server-rendered poster with no layout shift, which the three component reproduces by rendering the poster `<img>` on the server and fading the canvas in over it; and a tuned default look (neutral IBL, soft shadow, tone mapping) that will take an hour or two to match with `RoomEnvironment`. **Timing:** the swap is *not* part of the spike and not before it. A hero of grey proxy boxes is worse than the baked car we have, so model-viewer stays on the landing page until the MA chassis and the visible-slot generators render a recognisable car; the commit that makes the hero a default MA build through the scene component also removes model-viewer, its CDN script, the preconnect hints and the `isCustomElement` entry in `nuxt.config.ts`. Part pages preview a single part GLB through the same component; share pages (M2) use the canvas snapshot, not a live viewer. If AR is ever wanted, model-viewer returns on share pages only, against the baked export M3 already plans.

#### Sockets without a GLB

§5.2 puts the sockets in the chassis GLB as named empties. Until that file exists the spike builds the same object in code: one `Group` per socket, **named by slot id**, positioned from a hand-authored table for MA. `wheelbaseMm` (80) and `treadFrontMm` (59.5) are already on the chassis record, and the regulation envelope (105 × 165 × 70) bounds the rest; nothing needs measuring. Mirrored slots get `-l`/`-r` groups exactly as §5.4 specifies (`roller-front-l`, `roller-front-r`), and both map back to the one slot id on click. The table is a TypeScript constant beside the component, **not** catalog data: ~~it is a stand-in for the GLB's empties and is deleted when they land~~ it was a stand-in for the GLB's empties, and since no GLB is coming (§5.6, 2026-09-16) it is the permanent socket source, one table per chassis beside that chassis' generator; putting it in `data/chassis/ma.yml` would create precisely the second data model §5.4 says not to have. The attach code — find the group by slot id, clear it, add the proxy — is what the GLB will use unchanged. The only thing that swaps later is where the groups come from.

Only slots you can see get a socket: `body`, `motor`, `wheel-front`/`-rear`, `tire-front`/`-rear`, `front-stay`, `rear-stay`, `side-stay`, `roller-front`/`-rear`/`-side`, `brake`, `damper` — 14 slot ids, about 24 sockets. `gear-set`, `terminal`, `switch`, `axle`, `bearing` and `fastener` draw nothing and stay list-only, which §5.4 already says. ~~`gear-set`~~ **Amended 2026-09-18 (owner):** the gears draw, because a lifted shell shows them; `gear-set` has a socket at each axle and a single-shaft chassis' `counter-gear` one at its motor. Wheel and tire share a position, so the tire proxy is a ring around the wheel proxy and the two are the first thing the touch test judges: if a phone cannot tell them apart, a tap on a wheel offers a two-button choice (wheel / tire) before the picker opens. That pattern is needed later anyway for a stay that carries rollers.

Proxy sizes are **category defaults** in the spike (roller ⌀13 × 5, wheel ⌀24, tire ring ⌀26, stay 60 × 2 × 20, motor 20 × 15 × 30, body a 150 × 35 × 40 ghost). Sizing from `specs` — `rollerDiameterMm` is on 41 parts, `wheelDiameterMm` on 9 — is a one-line upgrade afterwards; it changes how the pane looks, not what the spike risks.

#### What a proxy shows, and how it is hit

- **State, by colour.** ~~The same three origins the list's badge already distinguishes: chassis grey, kit blue, user orange.~~ ~~Stock blue, changed orange, matching the list's `swapped` once the origin badge went (2026-09-16).~~ **Amended 2026-09-18 (owner):** a filled slot is drawn in the colour of what fills it, stock or changed alike; orange said *that* a part changed and never *which*, and the list row already says what changed. Ghost outline when the slot is empty. Emissive tint on hover and while the slot's picker is open, so the canvas and the list agree on which slot is being filled. No text in 3D: it needs fonts, and the list names the part 200 px below. "Watch the car update" in the spike therefore means *the box changes colour when you swap*. That is the honest floor and it is enough to test the loop end to end.
- **Hit volumes are not the visible boxes.** A 13 mm roller on a 165 mm car filling a 360 px phone screen is 28 px, under the 44 px minimum tap target, so each proxy carries an invisible hit sphere about twice its visible size and the raycast tests the spheres. Whether 2× is enough is the phone test's job.
- **Tap versus drag.** Pointer events, with a ~6 px movement threshold between `pointerdown` and `pointerup`; without it every orbit ends in an accidental pick. Orbit, zoom and a reset-camera `<button>` are the whole camera UI.

#### Where it loads, and what it must not touch

- `app/components/build/Scene.client.vue`, mounted from `build.vue` as `LazyBuildScene` under `v-if="chassis"`. Three is fetched only once a build exists — not on `/`, not on `/parts`, not on the kit picker — and the `.client` suffix keeps it out of the server bundle, so `nuxt generate` prerenders `/build` exactly as it does now. `@types/three` (0.186.0) joins devDependencies so `typecheck:app` covers the component.
- **Verify the split, do not assume it:** `dist/build/index.html` must not `modulepreload` the three chunk, and the prerendered HTML of `/build` must be byte-identical apart from the canvas placeholder. Grep both.
- The canvas sits above the slot list in the 800 px content column, 4:3 on phones and 16:9 wider. The 3D/list preference toggle (§5.4) is **not** in the spike; both surfaces are simply shown. On the seven other chassis: no canvas, one line saying 3D is MA-only for now, in both locales. The still image for those chassis waits on the chassis image scrape (§6 M1b).
- Accessibility: the canvas is `role="img"` with a label saying what it is and that the list below is the accessible path; reset-camera is a real button. Hover has no keyboard equivalent by design — §5.4 makes the list the keyboard surface.

#### Steps, riskiest first

1. **Empty lit canvas, split and measured.** Install `three` and `@types/three`; a canvas that renders a lit box behind `LazyBuildScene`; `npm run generate`; confirm prerender still passes, confirm the chunk is separate, record the chunk's gzipped size and the route's new total next to the table above. *Kill point:* if the chunk cannot be kept off the route's preload, stop and rethink before any scene work.
2. **MA sockets and proxies.** The socket table, one group per socket named by slot id, category-default boxes, orbit / zoom / reset. The scene re-populates from the resolved `slots` on change, colour by origin.
3. **Pick.** Raycast over the hit spheres, hover tint, tap-versus-drag threshold, `emit('select', slotId)` → `openSlotId` in `build.vue` → the existing picker. The open slot's proxy stays highlighted until the picker closes.
4. **Phone.** Owner tests on a real device: tap a front roller, tap a side roller, orbit without picking, tell wheel from tire. Record hit rates and whether 2× hit spheres are enough.
5. **Write it back.** Sizes, tap results, the TresJS-or-plain decision and what it cost, into this section and §6 M1c.

**Not in the spike:** any mesh, generator or body; spec-driven proxy sizes; the 3D/list preference; the other-chassis still image; ~~PostHog events on the canvas~~ (done 2026-09-18, §6 M0). Roughly 6–10 h of M1c's 55–75.

**Done when:** on MA, a reader taps a grey roller box on their phone, the roller picker opens, they choose one, and the box turns orange — with `/parts` and the landing page no heavier than today.

#### Built and measured (2026-09-16)

Landed the same day it was scoped. `app/components/build/Scene.client.vue` is the pane, in plain three; `shared/scene/sockets.ts` is the MA table (23 sockets over 14 slot ids, tested against the slot profile); ~~`shared/scene/chassis.ts` is the set of chassis the page reserves a pane for~~ (gone since every chassis has a pane, 2026-09-17, §5.6). `build.vue` mounts the scene as `LazyBuildScene` under `v-if`, and a tap goes through the same guard as the row's Swap button.

**Bytes.** The three chunk is **137 KB gz** (the esbuild estimate above said 140). It is not in the server bundle, `nuxt generate` prerenders the same 29 routes, and neither `/` nor `/parts` references it. `/build` carries it as `<link rel="prefetch">` — Nuxt prefetches a route's dynamic imports at idle, which is a download but not a parse, and it does not delay the page; leave it unless the analytics say most `/build` visitors never start a build. JavaScript **modulepreloaded** on `/build` went 114.8 → **115.8 KB gz**, the page's own growth.

**The one surprise was a shared module, not three.** The first build put 126.8 KB on the preload list: +12. Two rebuilds isolated it — neither the lazy component alone nor the socket-table import alone did it, only both. `build.vue` and the lazy scene both imported `sockets.ts`, so the bundler had a module shared between the route chunk and a lazy chunk, and it placed that module in a chunk already shared elsewhere: the @nuxt/content prose components, 10.7 KB gz, which the route then had to preload for a helper it never calls. The page then read `SCENE_CHASSIS` from its own module and never touched the table, with `sockets.test.ts` holding the two in agreement (both gone since 2026-09-17, when every chassis gained a pane and the page stopped importing anything from `shared/scene`). General rule worth keeping: **a module imported by both a route and one of its lazy chunks is placed by the bundler, not by you**, and the placement can cost more than the module.

**Taps, measured on a phone-sized frame.** Chrome touch emulation at 390 px, the 4:3 pane at 326 × 245 CSS px, the kit Stier, a tap dispatched every 10 px and the picker it opened recorded. Equivalent diameter of each target's hit area (per socket where a slot has two):

| Target | px | | Target | px |
|---|---|---|---|---|
| Body (roof only, see below) | 81 | | Front stay | 53 |
| Front roller (each of two) | ~52 | | Front tire | 53 |
| Motor | 56 | | Front wheel | 37 |
| Damper (each of two) | ~40 | | Side roller (each of two) | ~35 |
| Side stay (each of two) | ~30 | | Rear roller (each of two) | ~30 |
| Rear tire | 32 | | Rear wheel | 23 |
| Rear stay | 23 | | Brake | 16 |

The near side clears the 44 px minimum; the far side does not, and cannot from a fixed three-quarter view — the reader orbits to it, which is what the hint says. A rear camera preset (M3 already lists presets) would make the rear a tap away rather than a drag away. Wheel and tire separate as designed, band against hub, 2:1 in the tire's favour. **A real thumb has not tried this yet**: emulation exercises the code path, not the finger, so the owner's device test is still step 4.

Three things the measurement changed, none of them predicted:

- **The body stole taps.** A hit box the size of the body took 62 of 81 taps aimed at a front wheel and the top third of those aimed at a front roller: it is the largest thing on the car and in front of everything from most angles, and the first proxy table had it 148 mm long, reaching over the roller corners. The body is now 130 mm, its hit volume is the roof alone, and the raycast prefers any other part along the ray — it is translucent, so the wheel behind it is what the reader is looking at.
- **The home view fitted a sphere, so the car filled half the phone.** A car is three times wider than tall; fitting a bounding sphere fills the frame's height and wastes a third of its width. The fit is now by width and height separately, per aspect, and reset returns to it. That alone grew every target by 20–45%.
- **Reset drifted.** OrbitControls' damping keeps applying the last drag's momentum after `reset()`, so a reset pressed while coasting landed off home. Spending the momentum with damping off first fixes it.

Also: ~~the body is always drawn at 45% opacity, or it hides the motor and the inside of every wheel~~ (opaque since the shapes landed, and lifted to see inside, §5.6); the tap-versus-drag threshold works (a drag across a roller opened nothing, a tap on it opened the front-roller picker with 45 candidates, choosing one turned both mirrored rollers orange); and the scene draws only when the camera or the build moved, so a static pane costs nothing between interactions.

**Spec-driven sizes followed the same day.** A roller or wheel is drawn at its record's `rollerDiameterMm` / `wheelDiameterMm` when it has one (41 rollers, 9 wheels), and a tire — no tire records a diameter — is a 6 mm band around whatever wheel is in its socket; the hit volumes scale with them. A slot with no spec keeps the measured defaults, so nothing above moved. ~~What it does not do yet: lift the axle for a large-diameter wheel, so a ⌀31 front on ⌀20 rears dips below the tray rather than raising the car.~~ (Lifted since the generators landed, §5.6.) The pane takes the page's part map as a prop for the specs alone.

**Still not done, as scoped:** the 3D/list preference, ~~the other-chassis still image~~ (moot: all eight chassis draw since 2026-09-17, §5.6), ~~PostHog events~~ (done 2026-09-18: `scene_ready` and `scene_tap`, §6 M0). ~~**Next in M1c:** the MA chassis GLB with slot-named empties, which replaces the socket table for MA and nothing else in the pane.~~ **Re-ordered 2026-09-16 (§5.6):** the visible-slot generators come before the chassis, and the chassis tooling is decided after them.

### 5.6 Where the shapes come from: scripted in the browser, not modelled (decided 2026-09-16)

With the pane built, the next M1c chunk is the shapes themselves, and the question was whether to model them — in Blender, driven by Claude through a Blender MCP session — or to script them. **Decided: parts and bodies are three.js geometry generated in the browser from catalog specs. Blender is not used for parts at all, and for the chassis only if primitives turn out not to be enough.** The reasoning, from the owner's framing of what the pane is for:

- **The goal is an instant impression of the build, not a reproduction of any part.** A reader should see a swap change the car's shape, and recognise a wide stay, a big roller, a low body. Sub-millimetre truth is not wanted, and §5.1's IP position wants stylised low-poly on purpose.
- **There is nothing to model *from*.** Every part has one Tamiya product photo; a kit has its box art, a painted three-quarter view; no scans, no multi-angle shots, no CAD. Nothing built in Blender from that would be more correct than a cylinder at the record's diameter. This removes the one argument Blender had for parts.
- **"Reuse one shape where two parts look alike" is what a parametric generator *is*.** A roller is one function called with 41 diameters; a plate is one function with 19 thicknesses. In Blender that is 41 exported files, or a hand-kept table from item numbers to a few shared GLBs — the second data model §5.4 already refuses. The catalog carries the parameters (`rollerDiameterMm` on 41 parts, `plateThicknessMm` on 19, `wheelDiameterMm` on 9; no tire has one, so a tire stays a band around its wheel until a spec exists), and `Scene.client.vue` already reads them.
- **Bytes.** Loading GLBs costs `GLTFLoader` plus a meshopt decoder, about 34 KB gz on top of the 137 KB chunk, before the first model exists, then one fetch per distinct model. Generators are code in the chunk the pane already pays for — a few KB for all nine visible kinds.
- **A generator can do what a file cannot.** It knows the diameter, so it can lift the axle for a ⌀31 front wheel — the dip §5.5 records — and size the tire around whatever wheel is in the socket.
- **Blender MCP produces artifacts, not a pipeline.** It runs Python in a live Blender session; what comes out is a GLB and a conversation. A `bpy` script committed to the repo and run with `blender -b --python` is reviewable and re-runnable; a session is not, and the installed Blender (5.2 LTS today) will drift under it. If Blender is used at all, the model lives as a script in `scripts/models/`, and MCP is at most a way to iterate on that script.

**Bodies follow the same rule, but per kit.** The owner's requirement (2026-09-16): a shell should briefly resemble the car on *that* kit's box art, not accurately, but so that a reader who has the box recognises it. Three generic archetypes do not do that, and a Blender model per kit is 305 of the most expensive asset there is, so the answer is the same one the parts got — one generator, many parameter sets. A body is a **silhouette**: a side profile (nose height, hood line, windscreen position and rake, roof height and length, tail height), a plan-view width at nose, cabin and tail, and a few flags (rear wing, open wheels or fenders, canopy). About a dozen numbers, lofted by one three.js function; drawn at 45% opacity as §5.5 already does, the silhouette is all a reader sees anyway, and it is what box art shows most clearly. The numbers are authored by looking at the box art, which we have for every kit, and live in a shape table under `data/` keyed by a body id; a kit names its `bodyShape` through `data/overrides/kits.yml`. Kits share entries: a recolour or a chassis re-release of the same car is the same shape, so 305 kits are roughly 150 silhouettes, and a kit with no entry falls back to a wedge. Colour is the other half of recognition, so ~~`catalog:thumbs` samples a dominant colour from the box art onto the kit record~~ the kit record carries `colours` (body, wheel, tire) and the shell is tinted with it. **Amended 2026-09-18:** read from the wiki's `Body color` / `Wheel color` / `Tire color` rather than sampled — box art is decals and paint on a studio background — and checked against the box art, with `data/overrides/kits.yml` covering the 13 kits no wiki row reaches, one row the wiki files under the wrong item number, and 23 unpainted polycarbonate "Clear" shells that take the colour their box paints them. **Amended again 2026-09-18:** a body the wiki leads with Clear, Translucent or Smoke (and a body part named a clear body set) carries `clear`, and the pane draws it see-through at 65% even seated — it is what the plastic is; the painted polycarbonate shells above take `clear: []` in their override and stay opaque. The chassis is coloured the same way: `colours.chassis` from the wiki's `Chassis frame` (an MS chassis' `Center color`, with `chassisEnds` for a nose and tail that differ), `aParts` from `Chassis A parts` (gear covers, motor cover, switch), and `clear` where either is clear plastic. A kit with no wiki row keeps the black chassis. Authoring order follows the pane: MA's 69 kits first, the rest as the other chassis gain a pane. IP: a dozen-number loft is far below §5.1's "faithful reproduction" rung — it carries no surface detail, no decals and no mould feature — but it is deliberately *recognisable*, so the "representative shape" label and the fan-site disclaimer stay.

**Shape of the code.** `shared/scene/generators/` holds one function per `ProxyKind`, taking the specs and returning a `BufferGeometry`; `Scene.client.vue`'s `VISIBLE` table calls them and its `HIT` table does not change, so §5.5's tap measurements stand. The generators import three at module level, and only the lazy scene component imports them, so they stay in the 3D chunk and nothing on `/parts` or the payload grows — the same placement rule §5.5 learnt from `sockets.ts`. Geometry runs in Node, so each generator gets a bounding-box test beside `sockets.test.ts`. Order of work: rollers, wheels and tires with the axle lift (the most specs and most of the silhouette); then stays, brake, damper and motor; then the three body profiles and the box-art colour. Record the chunk size before and after, and re-check that no route loads Zod.

#### Built and measured (2026-09-16, first cut)

The generators, the MA chassis from primitives and the first silhouette landed the same day, on the Blast Arrow (18635) as the trivial kit: `shared/scene/generators/{mesh,parts,body,chassis}.ts`, the silhouette table in `shared/scene/bodies.ts`, and `Scene.client.vue`'s `VISIBLE` table now calling them while `HIT` is untouched. What the first look changed:

- **A single tub cannot be a Le Mans prototype.** The first loft was a six-cornered tub per station; against the Blast Arrow box art (two front fender pods either side of a low nose, a bubble cabin, a wing on pylons) it read as a generic wedge, which is what the owner said. The section is now eight-cornered — floor, shoulder, deck, cabin — and a silhouette also carries mirrored **fender pods** and a **wing with pylons**. Blast Arrow is 9 hull stations, 5 pod stations and one wing line: about 70 numbers, authored in a few minutes from the box art. Whether that format holds for the next kits is what decides when the table moves to `data/`.
- **A kit is a livery as well as a shape.** The stock tint the scene draws a part in used to be one blue for every stock part; a stock kit now takes natural colours per kind (black tires, dark FRP, silver motor), and the silhouette entry can name the kit's body, wheel and roller colours — Blast Arrow's white fin wheels and blue rollers are most of what makes it look like the box. Changed parts stay orange, empty slots translucent grey. **Amended 2026-09-18:** every part record carries `colours` too — `primary`, plus `tire` for a wheel-and-tire set — derived from the name, the wiki variant or the material (`scripts/catalog/colours.ts`) and set from the product photo in `data/overrides/parts.yml` where that reads nothing or wrong (195 of 382); a motor's colour is its end bell. The pane draws a part in its own colour whether stock or changed, and orange is gone. Cost: the builder payload went 79.5 → 85.7 KB gzipped; packing the colours into strings measured 83.9 and was not taken. The 3D chunk is unchanged at 142 KB gz, and no route loads Zod. **Amended again 2026-09-18 (owner): the motor is modelled, not a proxy.** There are only two motor shapes, the single-shaft FA-130 type and the double-shaft PRO, and what tells 32 motors apart is the end bell and the sticker (a Japan Cup edition is its base motor with a new sticker), so the shape can afford detail the other kinds do not: a flat-sided steel can (20.1 × 15.1 mm) with side vents and a front boss, the end bell, the clear terminal clip with brass tabs, and the sticker on the top flat with two bands of print, 424 triangles in eight draw groups (`MOTOR_GROUPS` in `shared/scene/generators/parts.ts`) drawn with a material each. Colours gained `sticker` and, for Plasma-Dash's black and Ultra-Dash's gold, `can`, all set from the product photos; a record with no sticker (AO-1001) draws a bare can, and print is dark or light by the sticker's lightness. **The gears followed the same day (owner).** `gear-set` draws at both axles and `counter-gear` at a single-shaft motor (`gearSet`, `counterGear` in `shared/scene/generators/parts.ts`): on a PRO chassis the full train at each end, pinion on the motor shaft, a counter gear with a crown face and a small spur, and the spur gear on the axle, between the housings (728 triangles an end); on a single-shaft chassis a crown gear beside each housing where the propeller shaft's bevel meets it (288), and the motor pinion with its counter gear on a steel pin beside the cradle (408). Teeth are small prisms on a revolved disc, so no curve code ships. A catalog gear set paints them its `primary`; a kit's gears carry no colour and draw a neutral moulding. Their hit boxes are moved off the motor's rather than centred on the socket, and taps on the gear, motor and wheel each still open their own picker.
- **Extrude cost 8 KB.** three's `ExtrudeGeometry` pulls its curve and triangulation code in; measured alone it is 70.2 KB gz against 61.9 for a box, so plates and the loft are hand-built triangles in `mesh.ts` instead. Every generated solid is checked for outward winding by signed volume in `generators.test.ts`, which caught three inside-out cases on the way. The review pass on this cut also caught a hover restyling the chassis floor white, because the restyle loop walked the chassis group as if it were a socket — the scene now keeps its own list of proxy meshes.
- **The 3D chunk went 137 → 141 KB gz** (150 with Extrude, 142 with three's lathe and merge helper, both replaced by a `revolve` on the same triangle helper); no route loads Zod; the payload is unchanged. The axle lift is in: a tire larger than the socket height raises the whole car.
- **The MA chassis from primitives** is a floor, arch cheeks, a battery bay and motor cover, bumpers cut to the real outline with roller posts, and side guards. It is enough to carry the sockets and to show the moulded bumpers of a stock kit; the roller posts are what made the rollers stop floating.

#### Second cut, the same day: an opaque shell you lift, and a chassis with its parts

The owner's read of the first cut, from a top view: the translucent shell and the translucent empty-slot ghosts together made the MA look like it had gaps, and a ghost read as a part that was half there. Three owner decisions followed, all landed:

- **The shell is opaque, and lifting it is how you see inside.** Tapping the shell no longer opens the body picker — the body is changed from its row in the list — it lifts the shell off the chassis, and a second tap seats it; a button in the pane does the same. The lifted shell eases up 24 mm and fades to 35% so it hides nothing of what it was covering, which is also what makes the motor and cells reachable by tap. Because the shell is opaque its own mesh is its hit volume, exactly what is drawn, and the "body loses to anything seen through it" raycast rule went with the translucency (§5.5's measurement of a box-sized body hit stealing taps was made under the old regime and no longer applies). One three gotcha paid for here: whether a material is opaque is baked into its shader program, so a material that flips `transparent` at runtime recompiles on the first frame of the lift, the most visible moment for a hitch. The shell's material is therefore its own, outside the cache, and always transparent — at opacity 1 that draws identically, and it is the only transparent thing in the scene, so there is nothing for it to sort against.
- **An empty slot is an outline, not a ghost.** Empties draw in wireframe, from a separate table of deliberately simple shapes — a box for a plate, an octagonal ring for a roller — because a wireframe of the real 16-segment revolve is a tangle. Edge-only outlines (`EdgesGeometry` and the line renderer) were measured at 8 KB gz and not taken.
- **Eight chassis is a bounded job, so each gets real detail.** MA is now a tub with side walls and wheel arches, the two AA cells lying either side of the motor with their terminal plates, gear housings at each axle, bumpers built as a crossbar, two arms and a centre rib so the real cut-outs show, roller posts, slotted side guards, the switch and the motor cover clip — about 680 triangles, all from the same box/prism/revolve helper. The review pass on this cut found that a naive sweep of a capped profile emits half its triangles with zero area (the axis points became rings of coincident vertices), so `revolve` now fans an axis point as an apex: a third fewer triangles scene-wide with no visual change. The same pass caught the render tick scheduling two frames per frame whenever the shell animated while the camera was still coasting, because the controls' change event and the lift each requested one; both go through the guarded request now. It reads as an MA from underneath and with the shell lifted, which is what the owner asked the top view to do.

The 3D chunk is 141 KB gz after all of it. **The posters were re-captured** from the built pane at the home view (the canvas element screenshotted at 2× and encoded at WebP quality 50: 32 KB for 16:9 and 13 KB for 4:3, up from 11 and 5 because wireframe outlines compress worse than flat boxes). Doing that showed what the empty car looks like as a first sight, and settled one more thing: **an empty body slot draws nothing.** A tap on the shell only lifts it and the body is chosen from its row, so there is no target to outline, and a wireframe loft over the whole car was the one outline that read as a tangle rather than a slot. Not done: the other 68 MA kits have no silhouette yet and draw the default wedge; a real thumb has still not tried the shell tap on a phone.

~~**The chassis is the one open item.** It is the plan's Blender asset and the only one where socket positions have to be true, but its reference is also photos. Decide once the parts look right against the tray: a raised centre tunnel and bumper bars from primitives may be enough for M1, and if not, a headless `bpy` script exports the MA GLB with its slot-named empties and replaces `sockets.ts` as §5.5 already describes.~~ Decided below.

#### All eight chassis, from the same primitives (decided 2026-09-16)

The owner's proposal, once the MA tub read as an MA: draw all eight chassis, because every kit and every build sits on one and there are only eight, and asked how detailed they should be given that a bounded set of eight could afford more triangles, or Blender. Evaluated the same day against §5.6's evidence. **Decided: all eight in M1, each a generator over the box/prism/revolve helper that drew MA, at MA's level of detail, no Blender, no GLB.** The seven move from M3 to M1c, and the still-and-notice fallback for them goes.

- **Poly count was never the constraint.** three draws a hundred thousand triangles on a phone without effort; what a chassis costs is code in the 3D chunk, and MA's ~680 triangles are about 2 KB gzipped of generator. Eight in code is under 20 KB in a chunk only the pane loads. The GLB route costs about 34 KB for `GLTFLoader` and the meshopt decoder before the first file, then a fetch per chassis (§5.6 above).
- **Blender would not be truer.** The reference for every chassis is photos — Tamiya's product shot and the chassis page — so Blender buys fillets and a visual loop, not more correct geometry. It also puts the site's most IP-sensitive asset on the wrong rung of §5.1's ladder: the chassis is Tamiya's core product and carries design rights, and a faithful mould is exactly what "stylised representative shape" is meant not to be. A helper that only makes prisms and revolves keeps the result stylised by construction.
- **Detail target: what a reader who owns that chassis recognises from below and with the shell lifted.** Motor position, battery layout, the bumper outlines with their cut-outs, side-guard shape, tread, gear housings. MA hit that at ~680 triangles; budget each chassis at 1–2k and stop when it reads. Anything past that is hidden under an opaque shell.
- **Grow the helper, not the assets.** Cells, motor bay, gear housing, `bumper(outline)` and roller posts become shared pieces, so a chassis is a layout plus one distinctive bumper outline. That is where MS, VS and Super-II differ in a reader's eye, and it is what keeps the eighth chassis cheaper than the second.
- **Sockets stay in `sockets.ts`, permanently** — one table per chassis beside its generator, in the same coordinate system, held to the slot profile by `sockets.test.ts`. §5.2's slot-named empties and the Blender pipeline are retired rather than deferred.
- **Order by kit count**, so each hour lands on the most readers: VS, Super-II and MS (50 kits each), then AR (34), FM-A (29), VZ (21), ME (2). Wheelbase and tread are on every record (`wheelbaseMm`, `treadFrontMm`, `treadRearMm`) and drive the axle sockets; the rest is authored from the photos in `data/raw/tamiya-chassis-images.json` and the regulation envelope, as MA's was. 2–4 h per chassis including its socket table, 20–30 h in all, on top of M1c's 55–75.
- **Bodies stay wedges outside MA for now.** Only MA's kits are queued for silhouettes (above); a drawn chassis under the default wedge is still far more than a placeholder, and the silhouette tables for the other chassis follow in the order their panes land. **Posters** exist for MA only; whether the empty pane on another chassis needs one, or simply draws on mount, is decided when the first of them lands.
- **Write back per chassis:** triangle count, the chunk size after it, and anything the helper had to gain. Re-check that no route loads Zod, as every 3D cut has.

**Built and measured (2026-09-17).** All seven landed in one cut, because the helper turned out to be the work and the chassis a parameter set each. `shared/scene/sockets.ts` now holds a `Layout` per chassis — wheelbase, treads, roller post positions, stay reach, side-stay x, motor position and whether the can lies across the car — and builds the 23 sockets from it; `shared/scene/generators/chassis.ts` builds each chassis from the *same* layout through a `Chassis` helper (floor or open frame, walls, a parametrised bumper with `rib`, `arms`, `vee` or `plate` fill and an optional nose, posts at the roller sockets, guards at the side-stay x, cells along the car at any x, gear housings, and a single-shaft drivetrain: motor cradle, crown housings and a steel propeller shaft). A chassis function is 10–15 lines of numbers. What each got: **MS** three floor pieces with seams and a ribbed battery cover; **ME** a tapering floor split at a flex line and a cage of bars over the motor; **AR** a sealed wide underbody with only the battery cover and motor hatch on it, an aero nose and long guards; **FM-A** the motor across the front, skid bars under the nose, a flat rear deck; **VZ**, **Super-II** and **VS** open frames with the cells visible through the ribs, the motor across the rear and each its own front bumper (arms, plate, V). Two things the scene needed: a socket can carry `rotateY`, which turns a single-shaft motor across the car (its hit box and outline turn with it), and the motor generator takes a shaft count, read from the same layout flag, so an FA-130 shows one shaft. The layout restates four catalog facts (wheelbase, both treads, motor shaft and position) so the 3D chunk carries no catalog record; `sockets.test.ts` reads each `content/chassis/*.yml` and fails if the layout stops matching it — the review pass on this cut found the shaft count first arriving as a page prop from the record, a second source for a fact the layout already held.

| Chassis | Triangles | | Chassis | Triangles |
|---|---|---|---|---|
| MA | 676 | | FM-A | 764 |
| MS | 724 | | VZ | 788 |
| ME | 712 | | Super-II | 764 |
| AR | 824 | | VS | 776 |

**Bytes.** The 3D chunk went 140.7 → **142.2 KB gz** for seven chassis: 1.5 KB, against the 34 KB a loader alone would have cost. The page's modulepreloaded JavaScript is unchanged (125.25 → 125.20 KB gz, measured against the previous commit's build); no route loads Zod. The pane gate is gone with the placeholder: `shared/scene/chassis.ts`, `hasScene`, the `build.scene.unavailable` string and its CSS, since every chassis now has a pane. MA's sockets and geometry are byte-for-byte where they were, held by a test, so the posters and the §5.5 tap measurements stand. Each chassis was opened in Chrome from its bare-chassis share hash and drawn without a console error. **Not done:** kits outside MA still draw the default wedge body, and a real thumb has still not tried a phone.

#### A body for every kit (planned 2026-09-18)

With the parts, gears and all eight chassis drawn, the wedge is the largest thing left on the pane: only Blast Arrow (18635) has a silhouette, so 304 kits draw the default. The owner asked for a shell on every kit, which moves the silhouettes outside MA from M3 into M1c. Colour is already done — every kit record carries `colours` — so the work is shape alone.

**The job is about 196 shapes, not 305.** Grouped by the wiki article a kit's loadout comes from (`loadoutSourceTitle`), the 305 kits are 183 distinct cars plus 13 kits with no article; recolours and re-releases share a shape.

| Chassis | Kits | Bodies | No article | | Chassis | Kits | Bodies | No article |
|---|---|---|---|---|---|---|---|---|
| MA | 69 | 42 | 3 | | AR | 34 | 20 | 0 |
| Super-II | 50 | 41 | 3 | | FM-A | 29 | 18 | 1 |
| VS | 50 | 34 | 3 | | VZ | 21 | 15 | 0 |
| MS | 50 | 28 | 2 | | ME | 2 | 1 | 1 |

Sixteen cars come on two chassis (Super Avante on VS and Super-II, Manta Ray Mk.II on MS and MA). Every wheelbase is 80–83 mm, so a shape carries across; the front tread runs 59.5–67 mm, which is where one may look too narrow or too wide. 272 of the 305 kits print their built size (全長 / 全幅 / 全高) in `specsRaw`, a free check on every shape. 19 catalog parts are bodies (clear body sets, Torcruiser and Veldaga body parts) and should draw the same shapes.

**Owner decisions (2026-09-18):**

- **The target stays §5.6's: a reader who owns the box recognises the car**, not merely tells it from its neighbours. Budget about 10 minutes a body.
- **Bodies stay one colour.** No canopy or window paint; `colours.body` is the whole livery.
- **A car on two chassis is one shape**, split into two ids only where it looks wrong on one of them.
- **The 13 kits with no wiki article are authored by eye** from their box art, not left on the wedge.

**Phase 0 — judge the format on six before copying it 196 times (4–6 h).** Blast Arrow took about 70 numbers; at that size the whole set is about 14k, so the format is settled first, on cars chosen to differ: a wedge (Avante Mk.III), an open-cockpit classic (Dash-1 Emperor), a truck (Terra Scorcher Jr.), a beginner shell (Raikiri), a flat wide AR-era body (Aero Avante) and one of the sixteen two-chassis cars. What to judge: open wheel arches, since many bodies show the tire and a one-colour shell makes the shape carry that; wing variants (on stays, or blended into the tail) and a nose fin; and whether a side profile plus a plan-view width line, sampled into stations by the generator, says the same car in fewer numbers than a record per station. The format freezes at the end of the phase, because a field added later means re-checking every body already authored.

**Phase 1 — shapes become catalog data (4–6 h).**

- `data/bodies/<body-id>.yml` holds one authored silhouette, keyed by a slug of the car (`avante-mk3`), naming the article and the kit whose box art it was read from, and validated by a Zod schema.
- Kits gain `body`: `catalog:generate` derives it from `loadoutSourceTitle` through one title → id table, and `data/overrides/kits.yml` covers the 13 untitled kits and any article that files two bodies under one title. The 19 body parts name theirs through `data/overrides/parts.yml`, so a swapped-in clear body draws its own shape.
- `catalog:verify` checks that every `body` id exists, that the shell winds outward (the signed-volume test `generators.test.ts` already runs), that it fits the 105 × 165 × 70 mm envelope, and, where a kit prints its size, that the shell is no longer than 全長 and its roof within a few millimetres of 全高. 全長 and 全幅 include bumpers and rollers, so they bound rather than match; the checks exist to catch a `280` typed for `28`.
- **Shapes load lazily.** The kit list is in every visitor's payload, so it carries only the id. All 196 silhouettes in the 3D chunk would add an estimated 20 KB+ gzipped to its 142, so they load through a lazy `import.meta.glob`, as hashed files fetched when a kit is chosen; until one arrives the pane draws no body, as it does for an empty body slot. Measure one file and the whole set before choosing a file per body or per chassis.
- `SILHOUETTES` and its colour fallback leave `shared/scene/bodies.ts`, keeping the wedge. The roller colour has no other home, so kit `colours` gains `roller` (wiki where it names one, overrides otherwise) or rollers fall back to the stock tint.
- Re-check that no route loads Zod.

**Phase 2 — authoring tools (3–4 h).** The reference is each body's full-size `officialImage`, fetched once into `.cache/bodies/` for authoring: never committed and never rendered, the same one-fetch rule `catalog:thumbs` keeps (§3.3). The 160×120 thumbnails are too small to read a profile from. A local script opens the builder on each kit's share hash, screenshots the pane at a three-quarter camera close to box art's, and writes a contact sheet under `.cache/` with the box art beside each render. That sheet is the owner's review surface, one page per chassis rather than 196 checks.

**Phase 3 — author in order of readers reached (30–40 h).** By chassis: MA (42), Super-II (41), VS (34), MS (28), AR (20), FM-A (18), VZ (15), ME (1) — §5.6's chassis order with MA first, since its pane and posters exist. Within a chassis, the cars with most kits first, and current kits (181) before limited ones (124). Per body: read the box art, write the YAML, generate, screenshot, correct once, and stop when it reads as that car. One commit per chassis after the owner has seen its contact sheet, each writing back here the bodies authored, the wedges left, the file sizes and anything the format needed. Subagents may author in parallel, but authors drift in style (how deep a nose, how tall a cabin), so each batch works from the Phase 0 bodies as references and passes through the same sheet.

**Risks.** A dozen-number loft is far below §5.1's "faithful reproduction" rung, but 196 deliberately recognisable shapes is more of a body of work than one, so the "representative shape" label and the fan-site disclaimer stay beside the pane. Box art is a painted three-quarter view with decals and perspective in the way, so heights and cabin positions are estimates; the 全高 check bounds how far off. The byte estimate is a guess until Phase 1 measures it, which is why the shapes load lazily rather than ride the chunk. **Total: 40–55 h**, mostly authoring, and the per-chassis commits make any chassis a place to stop.

#### Built and measured (2026-09-18): every kit draws its own car

All four phases landed the same day. **176 shells cover all 305 kits**, and the wedge is now only a fallback for a kit whose car has no file yet.

- **The format took three additions and no more** (phase 0, six cars chosen to be unlike each other — Avante Mk.III, Dash-1 Emperor, Terra Scorcher Jr., Raikiri, Aero Avante, K4 Gambol). Stations became rows rather than objects, because this is what ships. **Wheel arches are one number**, a radius per end, with extra sections sampled across the arch alone — sampling the whole hull split every flat face into slivers that shaded in a sawtooth. **Pods became a list**, so a car can carry fender pods, nose fins and rear fins at once. **A wing with no pylons** stands on its end plates down to the deck, which is how a tail flap sits on a tail, and both it and the pylons read the deck height interpolated at the wing's own z rather than at the tail. What the format still cannot say, from the authors' notes: nothing on the centre line (a pod is always mirrored, so a dorsal fin or a roll cage over the cockpit has to be faked as a close pair), no opening cut in a deck (an open cockpit or a pickup's bed floor reads as a low deck), and a pod is a straight loft, so a swept blade comes out straight.
- **The calibration that mattered was Tamiya's own 全高.** The body floor sits 14 mm above the ground, so a car's roof is `全高 − 14`; Raikiri was authored 9 mm too tall before that was written down. `catalog:verify` fails a shell more than 10 mm out, and it caught Tri Gale 12 mm tall — all three of its recolours print 36 mm, and the shell had a wing where the car has a low one.
- **Attachment is by article, with item numbers as the override.** A kit takes the shell whose `titles` holds the wiki article its loadout came from; `kits` lists the ones no article reaches — the 13 untitled kits, and the packs whose own name says what they are (the MA starter pack is a Blast Arrow, the AR one an Aero Avante, the Nero advance pack an Avante Mk.III on MS, the Pink Special a polycarbonate Raikiri). 17 clear and spare body **parts** name their car too, so a swapped-in body set draws it.
- **13 shells serve more than one car.** The whole MA "GT" mascot family is two shells, winged and not; Cat, Owl and Pig Racer are one; a car on two chassis (Super Avante, Manta Ray Mk.II, Mach-Bullet) is one shell, and none had to be split.
- **Bytes.** The shapes are 176 JSON files, 704 KB on disk, one lazy chunk each (~360 bytes), fetched only when a kit that draws it is chosen — confirmed in the browser: opening a Raikiri build fetched `raikiri.json` and nothing else. The **payload went 85.5 → 87.3 KB gz**: the ids alone, since @nuxt/content's devalue dedupes the 305 references to 176 strings. The **3D chunk went 142.2 → 143.9 KB gz**, of which 2.9 KB is the lazy-import table naming all 176 files; no route loads Zod. Geometry is 176–744 triangles a shell (median 496).
- **How it was authored.** `scripts/catalog/body-sheet.ts` draws a shell on its reference kit's chassis, in that kit's colours, beside the box art at the three-quarter angle Tamiya shoots from, plus side and top views with the axles marked, and writes a contact sheet per chassis; everything it writes stays in `.cache`. 19 subagents authored ten cars each from `data/bodies/README.md`, rendering and correcting once. Two lessons for the next batch job: agents working in one directory **overwrote each other's new files** when two batches reached the same car from different kits (DCR-01, Estoura — one hull was lost and rewritten), and a family that spans batches (the animal racers) cannot be merged by the agents that see only their own slice, so the merges were done centrally afterwards.
- **Not done:** a real thumb still has not tried the pane on a phone; the contact sheets have not been through an owner pass; and the shells are one colour, so a canopy or a two-tone livery is still only shape.

#### Wheels and tires that differ (planned 2026-09-18)

With every kit drawing its own car, the wheels under it are the next thing a reader looks at and the pane draws them all the same: one ⌀20 rim of 16 segments and a 6 mm band around it, on every kit and every chassis. Tamiya's own vocabulary is coarser than the bodies' and far smaller — a wheel is a size, a profile and a face — so this is a table, not an authoring campaign. Colour is again already done: all 305 kits carry `colours.wheel` and `colours.tire`, and all 53 wheel and tire parts carry `colours.primary` (plus `colours.tire` on the 35 wheel-and-tire sets), so the work is shape and size alone.

**The job is about 45 rows, not 305 kits.** Grouped by the wiki phrase the loadout carries, the 292 kits with a wiki row are **31 distinct wheel phrases and 12 tire phrases**, in 37 combinations — 43 rows, plus a default per chassis for the kits no wiki row reaches. Front and rear **never differ** across those 292 kits, so a kit is one wheel type and one tire type; the builder still lets a reader change one end alone, which is what the copy-from-the-other-end button is for. The 13 kits with no wiki row inherit their chassis' `defaultLoadout`, whose label is "Kit standard wheels".

| Kits | Wheel phrase | | Kits | Tire phrase |
|---:|---|---|---:|---|
| 42 | Small Low-Profile Saber-Type | | 134 | Small Low-Profile Slick |
| 32 | Small Low-Profile Fin-Type | | 72 | Large Avante-Type Slick |
| 24 | Large Narrow Lightweight | | 32 | Large Arched |
| 23 | Small Low-Profile A-Spoke | | 20 | Small X Narrow-Type |
| 20 | Small VS-Type | | 9 | Small High-Profile Slick |
| 19 | Large MS II | | 9 | Large Low-Profile Slick |
| 18 | Large Manta Ray-Type | | 6 | Large Avante-Type Spiked |
| 14 | Small Low-Profile Y-Spoke | | 5 | Small Treaded Radial |
| 14 | Large MS I | | 5 | four phrases on 1–2 kits each |
| 86 | 22 phrases on 1–9 kits each | | 13 | (chassis default) |
| 13 | (chassis default) | | | |

**Owner decisions (2026-09-18):**

- **Size, profile and face, not tread.** Correct diameters and widths, the tire's profile and shoulder, and a real face per wheel type — 5/6/12-spoke, Y, V, A and T spoke, dish, mesh, fin, saber, spiral, angular. Spikes, rally blocks and radial treads stay off: they are a dozen more rows of geometry for a pattern that is a few pixels at pane size, and the phrases that need them reach 12 kits between them. They can be added to the same rows later without re-authoring anything.
- **The builder is the only surface.** The 53 wheel and tire parts get shapes so that swapping one into a build draws it, exactly as the 17 body parts name their shell. `/parts/<id>` pages keep the photo and the specs table; a 3D preview on a part page stays M3, where §5.4 left it.
- **The shapes are code, not catalog data.** 43 rows read only by the generator, with no locale and no reader-facing text, are sockets-scale, not bodies-scale (§5.5: "a TypeScript constant beside the component, **not** catalog data").

**What is wrong today, and worth fixing on the way.** `wheelDiameterMm` is parsed from any `NNmm` in a part's name (`scripts/catalog/taxonomy.ts`), and in all 9 parts that have it the printed millimetres are the **tire's** outer diameter — "SMALL DIA. LOW FRICTION LOW-PROFILE TIRES (26mm) & CARBON WHEELS (FIN)" is recorded as a ⌀26 *wheel*, which the pane then bands out to ⌀32. So the field is populated but means the other thing. Deriving `tireDiameterMm` from the phrase and keeping `wheelDiameterMm` for the rim also feeds the rule engine: §4.3's 22–35 mm tire rule is listed as "not checked yet" only because `tireDiameterMm` is set on **0 of 382** parts, and this sets it on every wheel, tire and set.

**Phase 0 — settle the format on six wheels and three tires (2–3 h).** Chosen to be unalike: Small Low-Profile Saber-Type (42 kits), Large Narrow Lightweight (24), Large MS II (19), Small VS-Type (20), Large Flat Dish (4) and Large Low-Profile 6-Spoke (8); Small Low-Profile Slick, Large Arched and Small X Narrow-Type for the tires. What to judge, before 45 rows are written against it: whether spokes read at all at pane size or turn to moiré on a phone; whether a face needs its own draw group (a plated or carbon rim beside a plastic hub) or stays one colour; whether the wheel and the tire still separate under a thumb once the tire is its true diameter rather than always 6 mm proud (§5.5's touch test, wheel 37–53 grid cells today); and how much of the face survives being mostly hidden inside the arch of a body that now has one.

**Phase 1 — the table and the wiring (3–4 h).**

- `shared/scene/generators/wheels.ts`: a `WheelShape` and a `TireShape` record per id — outer diameter, width, offset, face style and spoke count, rim depth, hub; and for a tire, outer diameter, width, sidewall and shoulder radius. `parts.wheel()` and `parts.tire()` take a shape instead of a diameter, and `TIRE_BAND_MM` retires: a tire's size is its own, not the wheel's plus six.
- `catalog:generate` writes a `shape` id onto each wheel and tire loadout entry, normalising the phrase through `scripts/catalog/labels.ts` first — the same key `data/taxonomy/loadout-labels.yml` already translates on, so the aliases table that stops `Small Fin-Type Low-Profile` and `Small Low-Profile Fin-Type` becoming two rows serves both. `data/overrides/kits.yml` overrides per kit; `data/chassis/*.yml` carries each chassis' default for the 13 kits with no wiki row, authored by eye from that chassis' era.
- The 53 parts take a `shape` the same way, derived from the name by the rules that already derive their category, with `data/overrides/parts.yml` for what the names do not say. A wheel-and-tire set names both.
- `catalog:verify` fails on a `shape` id no table row defines, on a wheel or tire loadout entry with no shape at all, and on a tire outside the 22–35 mm envelope.
- **No lazy loading.** 45 rows of a dozen numbers is an estimated 1–2 KB gzipped in the 3D chunk against 176 shells' 704 KB on disk, so they ride the chunk and no fetch is added. Measure it in phase 4 rather than assuming it.

**Phase 2 — author the 45 rows (6–8 h).** In order of kits reached, tires first because three phrases cover 238 of them. The reference is the same one the bodies used — the full-size `officialImage` of a kit or of the matching Grade-Up Part, fetched once into `.cache/`, never committed. Real sizes are the fixed points every row hangs off: small ⌀24, small low-profile ⌀26, large ⌀31, large low-profile ⌀30, and the regulation floor and ceiling at 22 and 35.

**Phase 3 — the parts, and the finishes (3–4 h).** Map the 53 names onto the same ids (`LARGE DIA. LOW FRICTION LOW-PROFILE TIRES (31mm) & CARBON WHEELS (DISH)` is the large dish already authored in phase 2), and add the one thing a kit never has: a plated or aluminium rim is a `metal` finish rather than a colour, which `Paint` already carries and `FINISH[kind]` currently hard-codes to `plastic` for every wheel.

**Phase 4 — review and measure (2 h).** Extend `scripts/catalog/body-sheet.ts` with a wheel sheet: every shape drawn at the pane's own camera beside the product photo, one page for the wheels and one for the tires — 45 shapes is one owner pass, not 45. Then measure what §5.6 measures every time: the 3D chunk before and after, the payload, triangles per shape, and that no route loads Zod.

**Risks.** Two kits carry a compound phrase the wiki writes as one string (`Small / Low-Profile Saber-Type (1st Set) / Low-Profile Fin-Type (2nd)`); §4.8 already leaves those untranslated and they take an override here rather than a parser. A wheel is mostly inside a wheel arch now that every kit has a body, so face detail may be spent on something nobody sees — which is what phase 0 is for, and the reason spoke *count* matters more than spoke shape. Width and offset move the car's overall width, which §4.3 does not check and 272 kits print as 全幅; a wheel authored 3 mm too wide will not fail anything. **Total: 16–21 h**, and every phase after 1 is a table a reader sees immediately.

#### Built and measured (2026-09-18): every kit and every part on its own wheels

All five phases landed the same day. **Every one of the 305 kits and all 53 wheel and tire parts now draw the shape their own words name**, and the ⌀20 rim with a 6 mm band around it is gone.

- **The table is 46 wheels and 28 tires, not 43.** The kits reach 43 rows between them; the rest are fitments only a Grade-Up Part sells — a 3-spoke narrow, a 7-spoke aluminium disc, a spiked large — which the parts reach and no box ships. A row is four numbers for a wheel (diameter, width, spokes, dish) and three for a tire (diameter, width, shoulder), and `spokes: 0` is a plain dish, which is the whole difference between a dish and a mesh at this size.
- **The phrase is the key, and nothing is stored twice.** `shapeId` slugs the phrase `labelFor` already normalised, so `Large Low-Profile 6-Spoke` finds `large-low-profile-6-spoke` with no second mapping table beside the translation one. Writing the slug onto each kit entry as well was tried first and **cost 4.9 KB gzipped of every visitor's payload** to repeat what `label.en` already says; only a chassis' `defaultLoadout` carries a `shape`, because "Kit standard wheels" names none. The 43 phrases are held to a row by `catalog:verify`, so the runtime fallback is for a build that arrived from a URL rather than for the catalog.
- **A product name says its shape in four words, and the fifth is Tamiya's silence.** `scripts/catalog/wheels.ts` reads a size, a profile, a width and a face or tread out of a name and composes the most specific key the table holds, falling back a word at a time. The load-bearing rule is that **small is the unmarked case**: every large product says 大径 or LARGE DIA., so `LOW-PROFILE TIRE & WHEEL SET (Y SPOKE)` is the small one on the shelf. All 53 parts resolve, 38 distinct rows between them, with no per-item override.
- **The spec that was populated and wrong.** `wheelDiameterMm` was read from any `NNmm` in a part's name, and in all 9 parts that had one the millimetres are the *tire's*: 15544 is "LOW-PROFILE TIRES (31mm) & CARBON WHEELS", recorded as a ⌀31 rim, which the pane then banded out to ⌀37. The name-parse is gone; `tireDiameterMm` comes from the shape and is now on **53 parts where it was on 0**, and where Tamiya prints the millimetres `catalog:verify` holds the table to them — which is how the large low-profile row was caught at ⌀30 against Tamiya's own 31.
- **Tire diameter left the "not checked yet" list.** §4.3 listed the 22–35 mm rule there for want of data. Every tire now records one, `catalog:verify` fails the catalog if any is outside the envelope, and a rule in `rules.ts` could not fire on a build assembled from it — so the check lives at build time and the page stopped telling the reader it went unchecked.
- **Two things the shapes forced.** A wheel has a front and a back now, so the **left socket turns 180°** (`sockets.ts`) or every car showed a reader the inside of its near wheels. And a rim and a tire of the same width put the lip and the sidewall on one plane, which flickers as the car turns: rims are authored 1 mm narrower than their tires, and `tire()` takes the wheel it seats on rather than only its diameter so a mismatched pair still clears.
- **The cache is keyed by the numbers, not by the names.** Eleven of the 46 wheel rows are the same four numbers under different Tamiya names — `large-ms-ii`, `large-teardrop-type` and `large-fm-type` are one shape — and a tire reads only its rim's diameter and width, of which the rows hold ten. Keying the geometry cache by row id held a geometry per *name* pair: **1288 tire entries where 230 differ**, the same 14 KB buffer rebuilt for each of the thirteen 21×11 rims a reader tries. Signing a shape by what its generator actually reads collapses 46 wheels to 35 and 1288 tires to 230.
- **Bytes.** The **3D chunk went 143.9 → 145.6 KB gz** (the two tables and the new geometry), and the **payload +1.1 KB gz** — measured against the same build with the committed content and without the three new part columns, 92.4 → 93.5 KB gz, since the absolute figure has moved under other work since the body commit. No route loads Zod. Geometry is 216–360 triangles a wheel (mean 280) and 192 a tire, against 136 and 192 for the one wheel and one tire they replace.
- **What the review caught, and it was the tooling.** `body-sheet.ts` read `entry.shape` to find a kit's wheels — the field only a chassis default carries — so every contact sheet drew the default rim while its comment claimed otherwise. The fallback chain now lives once, in `entryShapeId`, and the scene, `catalog:verify` and the sheet all ask it. Two smaller ones from the same pass: a wheel's spec row printed `wheelDiameterMm`, which nothing populates any more, and now prints the tire size Tamiya actually names a wheel by; and the part-name reader skipped the pipeline's NFKC `normalise`, which three wheel names already need for their full-width brackets.
- **Not done:** tread is still shape-free — a spiked, rally-block or treaded-radial tire draws as a smooth shoulder, which the owner scoped out and which the rows can gain without re-authoring; the faces have not been through an owner pass against the product photos, and the ones marked `?` in the table are a judgement rather than a count; and a real thumb still has not tried telling a wheel from a tire on a phone, now that the tire is its true diameter rather than always 6 mm proud.

#### Switching the car on (planned 2026-09-18)

The owner asked for a button in the pane that switches the car on, so the wheels and gears turn. Three decisions the same day: the gears turn at the rates their **drawn** teeth give, not the kit's `gearRatio`, so the teeth visibly mesh (a real 3.5:1 against a 6-tooth pinion drawn at 14 teeth would show teeth sliding through each other, and a real motor's 20,000 rpm is not something a frame rate can show anyway); the button is there on the empty MA outline car too, where the wireframe wheels turn; and the switch slider moulded on every chassis slides when the car is on.

- **The gear trains become pieces.** `gearSet()` and `counterGear()` merge the axle spur, the counter gear and the pinion into one geometry, and those turn about different axes at different speeds. They return a list of rotors instead: each a geometry built about its own pivot, still in `GEAR_GROUPS`' two draw groups so paint and highlight are unchanged, with the axis it turns about and its turns per turn of the axle. The steel pins are a rotor that holds still.
- **The trains must actually drive the car.** A test places every rotor in the car's frame and checks that at each mesh the two surfaces move the same way, that the teeth pass at the same rate, and that the PRO motor's two pinions — one shaft — turn as one. Worked through before starting: **the PRO trains as drawn fail it.** They mirror front to back with both crown gears on the car's left, so one shaft would drive the axles in opposite directions; one train has to move to the other side, the way the real gear covers sit diagonally. The single-shaft crown gears have the same fault against the propeller shaft's two bevels.
- **One clock, one angle.** The pane still draws on request (§5.5); while the motor runs, each frame requests the next, the way a lifting shell does. Speed eases up over about 0.4 s and coasts down over about 0.8 s, then the pane stops drawing. Time steps are capped so a returning tab does not jump, and an `IntersectionObserver` stops the frames while the pane is scrolled away. The angle is the scene's, not a mesh's, so a kit swap mid-spin rebuilds the meshes without resetting them.
- **What turns:** wheels, tires and the gears; a mirrored socket turned 180° spins the other way in its own frame. **What does not:** rollers (they turn against a fence, not on a stand), shafts (an 8-sided cylinder turning looks like one standing still) and every hit volume, so tapping is unchanged.
- **Target speed** about 0.6 axle turns a second: the 6-tooth PRO pinion then turns under 30° a frame even at 30 fps, below where its 60° tooth pitch strobes backwards.
- The button sits in the pane beside reset and lift, with the lift button's `aria-pressed` pattern; it starts off on every mount. `scene_power { chassis }` counts switch-ons with no new GA4 dimension.

**Step 1 landed the same day: the trains are rotors, and they drive the car forward.** The test failed exactly where worked out — the rear PRO train, then both single-shaft crowns — and `gearSide` in `parts.ts` now puts each on its side, with the single-shaft rear housing moved to match and the scene's gear outline and hit volume following it. The single-shaft counter gear turns the propeller shaft through a crown the pane does not draw, so its direction is a choice rather than a check. Splitting a gear set into three or four meshes also exposed that `Triangles.grouped` emitted empty draw groups, each still a draw call in three; it skips them now, which leaves a single-shaft car one draw call *under* where it started. Nothing turns yet: step 2 is the clock, the button and the switch.

**Step 2 landed the same day: 開動 switches the car on.** The button is bottom-right in the pane, on every chassis and on the empty outline car. Measured in Chrome on MA (Stier, a 120 Hz display): 0 frames a second idle, one a display frame while on, and after 停車 the car coasts for about 1.7 s and the pane goes back to 0. Scrolled out of view it draws nothing while on, and picks up where it was when it returns. What the measuring changed: at the planned 0.8 s time constant, and a stop at 0.005 turns a second, a coast took 3.8 s of the pane drawing a crawl, so it is 0.5 s and 0.02. The switch slider is a chassis piece of its own now, keeping the A parts' colour, and slides 4 mm toward the nose. `scene_power { chassis }` counts switch-ons. **Not done:** `prefers-reduced-motion` is not consulted, since nothing moves until the reader asks for it; the 3D chunk was not re-measured for the ~40 lines this adds; and the single-shaft counter gear's direction is still a choice rather than a check, since the crown it drives is not drawn.

---

## 6. Roadmap

Re-targeted 2026-09-08. The owner set the MVP as: **start from a bare chassis or an existing kit, swap parts in a compatibility-checked list, and watch a 3D model of the result update.** That pulls the 3D view (F7) forward out of the old Phase 3 and pushes the wizard (F5) and community signals (F6) back behind it. §1's data-first ordering is not abandoned, it is spent — see guiding decision 1.

Milestones are renamed M0–M4 to make clear they are not the old Phase 0–4.

### M0 — Foundation (done)

- ~~Add PostHog, so the builder funnel has a baseline from its first day.~~ — done 2026-09-18, and GA4 moved to the same footing. Both tags now load through `@nuxt/scripts`' registry instead of a hand-written `<script>` in `app.head`, which buys three things: neither tag is in the prerendered HTML any more (`trigger: 'onNuxtReady'` injects them after hydration, so the critical path is unchanged from having no analytics at all), the call sites are typed and queue until the tag loads, and posthog-js is a lazily-imported chunk that no route modulepreloads. **Its size deserves recording next to the 3D chunk's 137 KB (§5.5): 94 KB gz, and unlike the 3D chunk it loads on all 1,709 routes, not just the builder.** The module's own runtime adds 10 KB gz, modulepreloaded everywhere, including a valibot consent schema this site never uses. Three decisions worth keeping, and one trap:
  - **`proxy: false` on both.** The module's first-party mode routes every beacon through a Nitro handler at `/_scripts/p/**` and anonymizes the IP there. `nuxt generate` output on Pages has no Nitro, and the module detects that and disables proxying by itself — but it is written into the config so that turning it on after the M2 move to Cloudflare Workers (§4) is a decision about where reader IPs go, not a side effect of changing host.
  - **`bundle: false` on GA, and not for the reason it looks like.** Bundling works fine on Pages: it is a build-time download written into `.output/public/_scripts/assets/<hash>.js`, a static file like any other, and it was verified serving from a plain file server. It is off because of what that file is — a copy of Google's gtag.js, which no licence lets us redistribute, frozen at build time on a script Google expects to update itself. That is the same test §1 decision 4 applies to the Tamiya thumbnails, and gtag.js fails it. Either way the beacons go straight to google-analytics.com.
  - **Pageviews are split between the two SDKs on purpose.** PostHog takes `capturePageview: 'history_change'`, which compares `pathname` only — the build and the category filter both live in the `#hash` (§4.6), so no swap is a pageview. GA has no equivalent, so `app/composables/useAnalyticsPageViews.ts` sends one `page_view` per navigation through `useScriptEventPage`, carrying `page_referrer` as GA4's SPA guidance asks, and skips the landing page because `gtag('config', …)` has already counted it. Verified against the generated output: one GA hit on load, one on navigation, `dr` set to the previous URL.
  - **Verifying PostHog in an automated browser does not work, and it is not a bug.** posthog-js drops every `capture()` before `before_send` when `navigator.webdriver` is true, which CDP-driven Chrome sets. Spoofing that getter turned the same call into a `200` from `/i/v0/e/`. GA has no such check, so the failure looks one-sided.
- ~~**The events themselves.**~~ — done 2026-09-18. Ten of them, one `track()` in `app/composables/useAnalytics.ts` sending the same name and the same properties to both SDKs, so a GA report and a PostHog insight are always about the same thing. `build_start` / `part_swap` / `part_revert` / `build_share` are the funnel; `rule_triggered` and `build_class_set` are §4.1's rule-warning frequency; `part_to_builder` and `part_no_slot` are the two ends of the part-page handoff; `scene_ready` and `scene_tap` close out the "PostHog events on the canvas" line in §5.5. Five things worth keeping:
  - **`autocapture: false`, and three CDN scripts with it.** What autocapture can name on this site is `button.link` and `a[href^="/parts/"]` — it cannot tell a roller swap from a tire swap or say which rule fired, because every action worth counting is a `@click` on a generic button. It would still have been the whole event bill. `capture_dead_clicks`, `capture_performance` and `disable_surveys` stop `dead-clicks-autocapture.js`, `web-vitals-with-attribution.js` and `surveys.js` downloading on every page. **Heatmaps survive** — they ride on `$$heatmap`, not `$autocapture`, which the usual summary of this trade gets wrong; verified in the built output.
  - **GA attributes every event to the page the session started on** unless told otherwise. `gtag('config', …)` resolves `page_location` once and every later event inherits it, so all ten would have been filed under whatever page the reader landed on. One `gtag('set', …)` per navigation in `useAnalyticsPageViews.ts` fixes all of them; there is no per-event parameter to remember.
  - **Both SDKs leak the build hash into their URL property, in opposite directions.** GA gets it because `useScriptEventPage` reports `route.fullPath`; PostHog gets it because posthog-js stamps `$current_url` from `location.href`, and `capturePageview: 'history_change'` only governs whether to *capture*, not what to record. Left alone, `/` becomes one report row per shared build on both. GA is handled in `useAnalyticsPageViews.ts`. PostHog is handled by `disable_capture_url_hashes` rather than a per-event override, because an override in `track()` can only reach our own ten events and the leak is worst on `$pageview`, `$pageleave` and `$$heatmap`, which the SDK builds itself.
  - **`chooseBase` is also the base row's Swap button.** Firing `build_start` on every call would count a reader changing their mind about a kit as a new build, inflating the exact numerator §5.5's 137 KB decision is measured against. It fires only when the bench was empty.
  - **Rule warnings are deduped per build, not per recompute.** `findings` recomputes on every swap, so reporting it straight would measure how much someone fiddled. A `Set` keyed by `rule:slotId` turns the metric into "share of builds where rule X fired" — the only shape a decision can be made on. **The clearing has to live inside the findings watcher, not in a watcher of its own**, which cost a review to find: two `pre` watchers on the same component are ordered by the order reactivity *reaches* them, not by the order they are written, and on the `null → kit` transition — how nearly every session starts — `findings` is reached first. The set was being wiped immediately after its own events fired, so every build's opening findings were reported twice. Reproduced in a 40-line Vue script before and after.
  - **Three more counting bugs the same review found, all of the same family — a guard that outlives or under-lives what it guards.** `scene_ready` measured from the page's `onMounted`, but the pane is keyed by chassis and remounts, so every car after the first reported how long the reader had been on the page rather than how long it took to draw — feeding garbage to the one metric §5.5 rests on. `build_start`'s `countedHash` was a closure, so opening a link, tapping to a part page and pressing Back counted the same build twice — and the rule-dedupe record had the same hole, re-reporting every standing finding on each return. Both live in `useState` now, because the add-to-builder funnel makes that round trip a routine move rather than an edge case. And `build_class_set`'s restore guard was armed before an assignment that is a no-op when the stored class is the default, leaving it armed for the life of the page and swallowing the reader's next real change.
  - **GA4 custom dimensions are a manual step and are not retroactive.** `chassis`, `slot`, `part`, `source`, `entry`, `rule`, `severity`, `build_class` and the `swaps` / `ms` metrics have to be registered under Admin → Data display → Custom definitions before any standard report will show them; events collected before then keep the parameter in the raw data but stay invisible in the UI. PostHog needs none of this, which is why both sides are sent the same property names.
- ~~**Make CI check something.**~~ — done 2026-09-16. Surfaced 2026-09-09 by the kit entry point, the first chunk whose weight sits in `app/`: the workflow ran `npm ci && npm run generate` and nothing else, and `app/` was type-checked by no command. The build job now runs `npm test`, `npm run typecheck` and `npm run catalog:verify` before `generate`, and `typecheck` covers `app/`. Three things worth keeping:
  - **`nuxt typecheck` does not work on this repo's TypeScript.** `vue-tsc` adds `.vue` support by patching the JavaScript source of `typescript/lib/tsc.js`; in TypeScript 7 that file is a launcher for the native binary, so vue-tsc 3.3.11 crashes (`ERR_PACKAGE_PATH_NOT_EXPORTED: './lib/tsc'`). npm `overrides` cannot nest a peer dependency either. `scripts/` stays on 7; `app/` is checked by `vue-tsc` on a `typescript6` npm alias through the few lines in `scripts/typecheck-app.ts`. Delete both once vue-tsc supports 7. Moving the whole repo to 6 was the alternative and was rejected: it gives up 7 everywhere for a problem only `.vue` files have.
  - **The first run found 10 errors, none of them a runtime bug yet.** Six were one gap: @nuxt/content types a collection from its Zod schema's *input*, where every `.default()` field (`mirror`, `required`, `stockLoadout`, `defaultLoadout`) is optional, while `shared/` types use the output. The generator writes every default out — 0 of 305 kits lack `stockLoadout` — so `fromContent` now takes the collection name and returns the schema's output type for the selected columns, and `catalog:verify` (now in CI) is what keeps that cast honest. Two were a union narrowing and a widened `rel: 'alternate'` in `app.vue`'s head; the generated hreflang links are byte-identical. One was the existing `as T` cast, which TypeScript 6 rejects. The tenth was in `nuxt.config.ts`, which `typecheck:app` covers by building the root `tsconfig.json` (app and node references) rather than the app config alone: the GA inline script used `children`, a prop unhead 3 has deprecated in favour of `innerHTML`. It still rendered, so analytics was never off, but it was a removal away from silently stopping.
  - **Run `nuxt prepare` before trusting any app/ error count.** A `.nuxt/` generated before i18n and the catalog collections landed produced 98 errors, 89 of them missing auto-imports and a `"content"`-only collection union. `typecheck:app` runs `nuxt prepare` first for that reason; in CI `npm ci`'s postinstall already does.
  
  Logic still belongs in `shared/` where it can be, but now because that is where it is unit-tested, not because it is the only place anything type-checks.
- ~~Push the four local commits (`8f41dbb`…`4d00ff3`)~~ — done 2026-09-08; the catalog is live.
- ~~Resolve branch layout: `main` + Actions deploy, retire the hand-managed `gh-pages` and the stale `master`~~ — done 2026-09-09; Pages reports `build_type: workflow` and only `main` remains.
- ~~Add `@nuxtjs/i18n` **before** any real routes exist, so `/parts/…` and `/build` are born with `prefix_except_default` rather than retrofitted~~ — done 2026-09-09 (§4.1, §4.6).
- ~~Nuxt 4 + @nuxt/content v3 scaffold, Zod schemas~~ — done, `83086af`.
- ~~Scraper and catalog pipeline; 382 parts + 8 chassis~~ — done 2026-09-08 (§3.2).

### M1 — MVP: the builder (the target)

Three workstreams. The catalog and builder ones are independent of the asset one, and the fallback proxy (§5.4) means the builder ships whether or not the meshes are ready — start them in parallel and let the 3D fidelity climb behind a working product.

~~**a. Catalog: kits (§4.7).**~~ — done 2026-09-08. 305 kits across the 8 v1 chassis, 96% with an imported `stockLoadout`, plus a hand-authored `defaultLoadout` on all 8 chassis. Nothing was hand-authored per kit.

**b. Site and builder.** Re-ordered 2026-09-09: the SEO pages moved from the head of this list to the foot of it. The plan's own reason for putting them first was that they are *cheap now that the data exists* — a marginal-cost argument, not a blocker, and nothing in the M1 done-criterion below needs them. The builder drives instead, so the catalog access layer is designed against the part picker's requirements (filter by **slot type**) rather than the browse page's (filter by **category**); the same query serves both, but only one of the two orderings produces a picker without rework.

- ~~`/build` bare-chassis entry point; slot list from the chassis' profile, each row showing its part, `stock` vs `swapped`, and a swap action; part picker per slot, filtered by slot type, chassis compatibility and motor shaft, searchable, showing price and the specs that matter for that slot~~ — done 2026-09-09. A build is stored as a **delta** (`chassis`, optional `kit`, `swaps` keyed by slot id), never as a resolved loadout, mirroring how a kit's `stockLoadout` is a delta over its chassis' `defaultLoadout`. Resolution is pure and unit-tested in `shared/catalog/build.ts`.
- Official product image on every row of every picker — parts, kits and chassis. **Kits done 2026-09-09** with the kit entry point: recognising your own box among 305 is a visual task, and the data needed nothing (305/305 already carry an `officialImage`). Remaining: the part picker, and the chassis scrape below. Parts and kits already carry an `officialImage` (100% of 382 and 305, Tamiya's CloudFront). Chassis carry none **in our data**, which is a gap in the pipeline rather than an absence of images: Tamiya publishes one per chassis on the [chassis select page](https://www.tamiya.com/english/cms/mini4wd_chassis_select.html), on the same CloudFront host as the part photos, and all eight in v1 scope are there. That page is currently consulted by hand (the `CHASSIS_CODES` comment in `shared/catalog/schema.ts` says "verified against" it) and never fetched, so this needs a small new scrape source — one page, eight images, into `data/chassis/*.yml`:

  | Chassis | Image | Confirmed by |
  |---|---|---|
  | ma | `/cms/img/usr/item/ch/519.jpg` | caption |
  | ms | `514.jpg` | caption |
  | me | `522_me.jpg` | caption |
  | ar | `518.jpg` | caption |
  | vs | `507.jpg` | caption |
  | vz | `520_vz.jpg` | caption |
  | fm-a | `fm__a.png` | caption |
  | super-2 | `517_s2.jpg` | caption |

  ~~The Fandom chassis articles are the fallback source.~~ **There is no fallback, and the wiki was never one** (checked 2026-09-16, §3.3): its chassis images are Tamiya's own photos uploaded under `{{Fairuse}}`, so they are not CC-BY-SA and are strictly worse than going to Tamiya — same rights holder, lower resolution.

  ~~All of them referenced from source, never re-hosted (§1, guiding decision 4), lazy-loaded, and degrading to the current text row when an image is blocked or gone — a hotlink we do not control must not be able to break a picker.~~

  **Done 2026-09-16, and the policy flipped on the way: nothing is hotlinked and every picture is ours to serve** (owner; §1 decision 4). The reasoning is in §1 — a hotlink costs Tamiya bandwidth on every view and shows our domain in their logs daily, which is what invites a block, while a copy costs them one fetch. So `npm run catalog:thumbs` fetches each photo once and writes a 160×120 WebP into `public/thumbs/{parts,kits,chassis}/<id>.webp`, `generate` records the path as `thumbnail` by reading that directory, and `catalog:verify` fails if a record names a file that is not there. **695 of 695 items have one** (382 parts, 305 kits, 8 chassis) at **2.7 MB total, ~4 KB each**, against the 24–94 KB each the hotlinked originals cost.

  Four things worth keeping:

  - **The thumbnail URL was already on disk.** This bullet used to call for a new scrape; in fact Tamiya's product *list* pages — 591 of them, already fetched and cached — render every row as `<img src="//…/cms/img/usr/item/1/15549/15549_s.jpg ">`, and `parseListPage` simply never read the `<img>`. The fixture in `scripts/catalog/__fixtures__/list-page.html` had the tag all along. Coverage is 100%, not the 19-in-20 the derived-URL measurement suggested.
  - **Deriving the URL would have been wrong, not merely unverifiable.** The thumbnail lives under `cms/img` while the full photo lives under `japan_contents/img`; kit 17901's thumbnail is `17901_s_04.jpg`, which the obvious `<id>_s.jpg` guess 404s on; and six of 1,138 items root their src at the site (`/cms/img/…`) rather than protocol-relative, which is what `absoluteUrl` in `sources/tamiya-page.ts` now normalises.
  - **The chassis photos come from the chassis select page, keyed by Tamiya's own code**, not by the caption: every `<li>` there carries `?genre_item=e_mini4wd_chassis_<code>` links, and a code is the identifier the rest of this pipeline already speaks. All eight resolved, and they match the table above, which had been transcribed by hand.
  - **The payload got smaller, twice.** Dropping the kits' full-size CDN URL took `/_payload.json` from 484.6 to 465.0 KB raw (88.4 → 84.9 gzipped). Then the path itself came out: it is `/thumbs/<collection>/<id>.webp` for every record, so the client derives it from the item number (`shared/catalog/thumbnails.ts`) and the payload carries `hasThumbnail` instead — 631 copies of a 25-byte string were 1.8 KB gzipped on every page. Final: **438.3 KB raw, 79.5 KB gzipped**, against 484.6 / 88.4 before any of this. `content/*.yml` still stores the real path, because that is what `catalog:verify` checks against the files. The picker row cap stays for DOM size, not bytes.

  What is still open: no takedown contact address is published yet — the credit line names Tamiya and disclaims affiliation, but the attribution page should carry an address before this gets much traffic.
- ~~**Localise `loadoutEntry.label`, and render the third origin (§4.8).**~~ — done 2026-09-09. Traditional Chinese reaches 88% of label entries (1,561 of 1,768); the shortfall is two compound free-text phrases the wiki writes as one string. All 15 motor labels became item numbers. `catalog:report` gained an untranslated-label worklist, and `scripts/catalog/labels.test.ts` pins the normaliser against the spellings the wiki actually produces.
- ~~`/build` kit entry point: kit picker searching by name or item number, overlaying `stockLoadout` on the chassis default~~ — done 2026-09-09. ~~Filtered to current kits~~ — **corrected 2026-09-09:** the corpus is 181 `current`, 124 `limited` and **zero** discontinued, so filtering to `current` would hide 124 kits people own. Show both and label the limited ones; the picker does, and does not rank one above the other. ~~Kit door primary, bare chassis the labelled fallback beside it ("can't find your kit, or starting from a bare chassis?")~~ — **amended 2026-09-09 by the owner:** a segmented control (`由套件開始` / `由淨底盤開始`) with the kit door selected by default. Two visible doors with a default, not one door and a footnote. Search matches the item number and *every* locale name, not the displayed one, which matters here more than in the part picker: two thirds of kits have no Traditional Chinese name, so "Raikiri" has to find a row reading 雷切. A chassis chip row (MA 69 … ME 2) narrows the 305. Kits go into the prerendered payload alongside the parts — **not** fetched client-side, where a browser-side `queryCollection` would pull 865 KB of SQLite WASM plus a 210 KB worker. Nothing on the site issues one today; do not be the first for this.

  **The payload estimate was wrong, and the reason is worth keeping.** The budget said "~25–35 KB over the wire". Measured on the generated route, 12 kit columns cost **+301 KB raw and +52.7 KB gzipped** (35.7 → 88.4 KB gzip; 183.7 → 484.6 KB raw). The same records as a standalone JSON array gzip to 23 KB, which is where the estimate came from — but Nuxt's payload format interns every repeated string into a table and replaces it with a numeric index, and that *inverts* which half is expensive. Of the 8,074 payload entries the kits add, only **1,809 are strings**; the other 6,265 are objects and arrays of unique integer references, which gzip cannot collapse the way it collapses `"Small Low-Profile Slick"` repeated 268 times. So the text is nearly free and the **shape** is the cost: 1,783 loadout entries × (an entry object + a label object + an array). Estimating a Nuxt payload by gzipping the equivalent JSON will under-count repetitive data every time.
- ~~**Rule engine (§4.3) inline, three severities.**~~ — done 2026-09-16. Ship the rules that the committed data can actually answer — slot capacity, chassis compat, motor shaft type, class legality, tire diameter — and leave the ones needing data we do not have (total width, precise weight) as explicit "not checked yet" rather than guessing. Chassis compat and motor shaft already filter the picker, so a build assembled in the UI cannot break them; they stay rules because a build arriving from a URL was not assembled in the UI.

  **Scoped 2026-09-16.** Counted before scoping, because the counts decide which rules can say anything:

  - **Open and Stock Class never differ on a part.** All 382 parts carry the same `open` and `stockBmax` verdict (345 legal, 2 illegal, 35 unknown), which matches the research: Stock Class keeps Open's motor list and replaces only the modification rules (`docs/research/concepts-and-rules.md` §1.2). The class that differs is **Junior**, Tune motors only, with 17 parts illegal. So the done-criterion's "the motor is Open-only" has no data behind it and is reworded below. The motor category blurb from `8869b9e` says "some motors are not allowed in Stock Class" in both locale files, which is wrong for the same reason, and is corrected in this chunk.
  - **Two §4.3 rules have no data.** `tireDiameterMm` and `weightG` are set on **0 of 382** parts, so tire diameter and minimum weight join total width as "not checked yet" rather than rules.
  - **Stock builds raise no false alarms.** Of 305 kits, none leaves a required slot empty and none carries a part its chassis does not list. One, 95598, has a Junior-illegal stock motor (Light-Dash), which is a correct finding. Every bare chassis leaves `body` empty.
  - **Slot capacity cannot be broken.** The picker writes one part per swap and `reconcileBuild` already cuts a link to `maxCount`, so a capacity rule would never fire. Dropped.
  - **`buildClass` already exists and nothing reads it.** `useBuild` holds it (default `open`) and `partsForSlot` ranks by it, but there is no selector, and `PartCard.vue` still says the MVP builds against no class.

  Three decisions, owner, 2026-09-16:

  - **The done-criterion becomes a class-illegal motor**, e.g. a Dash motor with the class set to Junior. Researching whether any part really is Open-only (B-MAX's factory roller combinations and gear pairings are the likeliest candidates) was rejected for M1, because it means more research and probably schema changes.
  - **The class stays out of the share link.** It describes the event the reader races, not the car, so a friend opening the link sees warnings for their own class. The link format stays at v1. The class is per-viewer state, and remembering it in `localStorage` is a convenience that must survive the storage being empty or throwing.
  - **An empty body is a warning, not an error.** Every bare-chassis build starts that way, and red there reads as a mistake. Other empty required slots stay errors. No UI path produces one; only a link can.

  **Code, not JSON rules, for M1.** §4.3 describes declarative rules with `message{locale}` in the data. There are four rule types, each needs part lookups that a rule format would have to reinvent, and the messages interpolate names resolved when the page renders. So rules are functions in `shared/catalog/rules.ts` and messages are `build.rules.*` i18n keys. Declarative rules wait for the contribution workflow (M4), which is what they were for.

  | Rule | Severity | Fires when |
  |---|---|---|
  | `required-empty` | error; warning for `body` | a `required` slot has no entries |
  | `chassis-incompatible` | error | `isChassisCompatible` fails; only a link can produce this |
  | `motor-shaft` | error | motor shaft ≠ `chassis.motorShaft`; only a link can produce this |
  | `class-illegal` | error | `classLegality[buildClass] === 'illegal'` |
  | `class-unknown` | note | the verdict is `unknown` (one car part today, 15340) |
  | `link-trimmed` | note | `reconcileBuild` dropped a kit, a slot or a part from the opened link |
  | `not-checked` | note, always, collapsed | 105 × 165 × 70 mm, ≥ 90 g, tires 22–35 mm, Stock Class / B-MAX modification rules; check the organiser's PDF |

  Shape:

  - **`checkBuild({ slots, chassis, partsById, buildClass, trimmed }) → Finding[]`**, with `Finding = { rule, severity, slotId?, partId? }`. It is pure, takes the same `ResolvedSlot[]` the list renders, and imports only types from `schema.ts` (no Zod on `/`). M2's server re-validation calls it unchanged.
  - **`reconcileBuild` reports what it dropped** alongside the build. `useBuildLink` passes that on, which closes the silent-trim gap the share-link work left open.
  - **`catalog:verify` gains an invariant:** every kit's stock build yields zero errors in Open. A bad loadout override then fails CI, not a reader's screen.
  - **UI.** A class selector beside the list title: 公開賽 Open · 基礎賽 Stock Class (B-Stock) · 少年組 Junior. A findings summary under the title, errors first, each linking to its row through the existing `slot-<id>` anchors. A marker on `SlotRow`, an icon plus text so it does not rely on colour. A badge on `PartCard` in the picker only when the part is not legal in the chosen class, which is what the `partsForSlot` comment already promises. `classLegality.notes` is not rendered, because it is Traditional Chinese only. Payload cost is zero, since `classLegality` is already selected; measure the chunk and grep the build for `ZodError`.

  Work, in order: `rules.ts` and `rules.test.ts`; `reconcileBuild` returning what it trimmed, and its tests; the verify invariant; the class selector and summary; the row marker and picker badge; the motor blurb and `build.rules.*` keys in both locales; a Chrome run; this section.

  **Done when:** an MA kit with a Hyper-Dash PRO swapped in shows no error in Open or Stock Class and one error in Junior; a hand-edited link putting a single-shaft motor on MA opens with the motor-shaft error on the motor row; a bare chassis shows the body warning; a link naming a part the catalog does not ship shows the trimmed note; `catalog:verify` passes with the invariant on all 305 kits; all of it holds in both locales, and no route loads the Zod chunk.

  **Landed 2026-09-16**, driven in Chrome against the generated site. A link to MA kit 17901 with a Hyper-Dash PRO shows no problems in Open or Stock Class and one error on the motor row in Junior, and switching class leaves the hash alone. After a reload the Junior choice is still there. A link putting a Hyper-Dash 3 on the same kit shows the motor-shaft error and the Junior error on the motor row. A bare MA shows the body warning. A link naming part 10000 in the motor slot and a motor in the brake slot opens as the kit, with the trimmed note, and its hash is normalised. In Junior the motor picker marks 7 of 14 motors as not allowed, and a finding's link scrolls to its row. `/en` reads the same. No console errors, `catalog:verify` passes on 8 chassis and 305 kits, no route loads `ZodError`, and the builder payload is unchanged at 81.9 KB gzipped, since no column was added. Four things worth keeping:

  - **The class selector was already half-built.** `buildClass` and the ranking in `partsForSlot` predate the engine, so this chunk added a selector to an existing state. `BUILD_CLASSES` and `isBuildClass` now live in `build.ts`, and a test pins them to the schema's `classLegality` keys, so a new class cannot be left out of the selector silently.
  - **`not-checked` is page copy, not a finding.** The plan's table listed it as a rule. A finding that always fires checks nothing, so `checkBuild` returns only real findings and `BuildFindings` renders the fixed list itself.
  - **A trimmed link is detected by comparison, not tracked.** `wasTrimmed(shared, state)` compares the parsed link with what `reconcileBuild` returned. That is sound because reconciling only ever removes things: a slot's parts are filtered and cut, never substituted, so equal counts mean equal contents. The reconciler keeps its single job. The note clears when the reader picks a new base.
  - **The shaft error wins over the chassis error on one row.** A single-shaft motor on a PRO chassis is usually also missing from Tamiya's list for that chassis, so `checkBuild` reports only the shaft. The class verdict is a different question and still gets its own line.

  **Future work, not scoped (owner, 2026-09-16): filter the picker by the parts already on the car**, not only by chassis and motor shaft, e.g. wheels by tire diameter or rollers by stay. The catalog has no data to do this: no part-to-part `requires` relation, `wheelDiameterMm` on 9 parts and `tireDiameterMm` on none. It is a data job first, a picker change second, and the rule engine's tire-diameter rule is waiting on the same data.
- Totals: part count, ~~JPY and HKD cost,~~ estimated weight, ~~final gear ratio~~.

  **Final gear ratio, done 2026-09-18.** The base row printed `kit.gearRatio` whatever was in the gear slots, so a 3.5:1 kit with a 4:1 gear set swapped in still read 3.5:1 — a stale number on the one figure a beginner compares. `gearRatioOf` in `shared/catalog/build.ts` now answers it: the kit's own ratio while neither gear slot is touched (a bare chassis has none, since "Kit standard gears" names no ratio); once one is swapped, only the swapped parts count, because a gear set or a counter gear set decides the ratio by itself; and two swaps that disagree, a spare pinion, an emptied slot or a setting set leave it blank rather than falling back to the kit. It needed data first: **8 of the 22 gear parts carry a ratio, up from 3.** 超速 (Super Speed) is its own ratio, 3.5:1 on every chassis — the wiki groups all seven variants as one family — so `deriveSpecs` reads it from the name beside the printed `(3.7:1)`; ハイスピード is not (4:1 on MS, 4.2:1 as the single-shaft counter gear 15236, per tamiya.hk), so 15236 is an override. The two setting gear sets (15456, 15516) hold several ratios and stay without one. Checked in Chrome against the generated site with five links: a 4:1 kit, the same kit on HS-EX (3.7:1), on a gear-bearing spare (blank), a bare MA on a Super Speed set (3.5:1), and a VZ kit with only its counter gear swapped (3.7:1). Payload −35 B gzipped against a build of the parent commit, with the wheel fix below; no route loads Zod.

  **Part count and estimated weight are not done, and neither is a small job.** A part count is a packet count, and how many packets a mirrored or multi-count slot needs is the question `choose()` in `app/pages/index.vue` already defers, since guessing it would put four bearing packets in a build that needs one. Weight has data on 11 of 382 parts. Whether weight leaves M1 the way cost did is an owner decision.

  **Cost is out of M1** (owner, 2026-09-16). The site is Traditional-Chinese-first but not Hong-Kong-only: readers in Taiwan, Malaysia, Singapore and beyond pay neither Tamiya Japan's list price nor tamiya.hk's, and street prices differ from both by shop. A JPY + HKD total would be a precise-looking number that is wrong for most readers, and HKD covers only 80% of parts and 78% of kits besides. The catalog **keeps** `priceJpy`/`priceHkd` — they are facts, already scraped and free to carry — so nothing in the pipeline changes; what moves is showing a total, which waits for regional pricing (M4).

  **Per-row labels: JPY stays** (owner, 2026-09-16). The part and kit picker rows render `¥` and `HK$` labels (`PartCard.vue`, `KitPicker.vue`), and the part picker uses `priceJpy` as a sort tiebreaker (`shared/catalog/build.ts`). A single item's JPY list price is a reference point that means the same thing to every reader — Tamiya's own number, printed for 100% of parts and kits — which a summed "what you will pay" total is not. The tiebreaker stays with it. `HK$` is the regional number the total was dropped for, and it comes off the rows (and out of `build.vue`'s selected columns) — not done yet.
- Build encoded in the URL (§4.5); reload restores; copy-link shares. The state is already shaped for it: only the delta needs serialising. **Validate that `chassis` and `kit` agree on decode**, with the kit's own `chassis` field authoritative: `{chassis: 'ma', kit: <a VZ kit>}` is representable and invalid, no UI path can produce it, and a URL can. Slot ids are near-identical across chassis — PRO (MA/MS/ME) have 19, the rest those 19 plus `counter-gear` and `propeller-shaft` — so swaps mostly survive a chassis change and should be kept when the chassis matches, cleared when it does not.

  **Scoped 2026-09-16, and more than a share feature.** Since the builder became the home page it keeps the build only in `useState`, so a reload on the one screen the site has throws the reader's work away. The hash fixes that and the share link in one change.

  - ~~**Format: readable text, not a byte array.** `#v1:<chassis>:<kit|->[:<slot>=<id>,<id>|-]…`~~ **Packed bytes, base64url** (owner, 2026-09-16: a link is pasted, not read). Version byte, chassis index, kit item number as uint24 (0 for none), then per swap a slot index, a part count (0 for emptied) and each item number as uint24. A kit with five swaps is 40 characters, where the text form was ~100. The cost the text form avoided is real and is paid in a test instead: a link stores **positions** in `SHARE_CHASSIS` and `SHARE_SLOTS`, so both tables are append-only, `share.test.ts` pins them with a literal encoded link that fails on any reorder, and checks they cover `CHASSIS_IDS` and every id in `data/taxonomy/slots.yml`. The tables live in `share.ts` rather than being imported from `schema.ts`, which would put Zod in the page. Unpadded base64url ends in a letter, a digit, `-` or `_`, none of which chat linkifiers trim off a link the way they trim `. , ; :`.
  - **Hash, not query string** (compared 2026-09-16). GitHub Pages serves either. A query string is readable by a server, which is the only thing it offers, and that job belongs to M2's `/b/:id` short links (§4.5). Against it: Cloudflare's default cache key includes the query, so every distinct shared build would be a cache miss in M2; GA4's `page_location` includes it, so reports would split into a row per build, rewritten on every swap; and each shared build becomes a separate crawlable URL of one page. Moving it is two lines in `useBuildLink.ts` if per-build share cards off the long link are ever wanted.
  - **Two pure steps in `shared/catalog/share.ts`, tested like `build.ts`.** `encodeBuild(state)` and `parseBuild(hash)` are syntax only — a malformed or unknown-version hash is `undefined`, never a throw. `reconcileBuild(state, catalog)` applies the catalog: an unknown kit is dropped (a link from an older catalog becomes a bare-chassis build rather than carrying a dead id forward on every re-share); a known kit's chassis overrides the link's, and the swaps are cleared when that changed the chassis; a swap is dropped when its slot id is not on the chassis, and a part id when it is not a buildable part or does not declare the slot's type, and the list is cut to `maxCount`. Chassis *compatibility* is **not** filtered: that is the rule engine's to report, and filtering it here is exactly what would hide the case the plan says URL builds need a rule for.
  - **Wiring, client-only.** The prerendered page stays the empty car; the only new markup is the header wrapper round the list title. After hydration a valid build in the hash replaces the state, and anything else — no hash, a garbage one — is overwritten by the build already in state (client-side navigation, a locale switch). Every build change then `history.replaceState`s the hash — replace, not push, so the back button leaves the site rather than undoing part swaps one by one, and it passes `history.state` through because Vue Router keeps its own position there. No router navigation is involved, so neither scroll behaviour nor route watchers fire.

    **`location.hash` is empty in `onMounted` on a prerendered page, and that is Nuxt, not the browser.** Opened at `/#…`, the router sees an initial URL different from the payload's path `/`, hydrates against `/` with the hash stripped from the address bar (`hasDeferredRoute` in Nuxt's router plugin), and restores the real route on `app:suspense:resolve`, which fires after mount. Read once, the link silently opened the empty car; a `hashchange` dispatched by hand afterwards worked, which is what gave it away. `useBuildLink` therefore watches `route.hash` rather than reading `location.hash`, and the same watch covers a second link pasted into the same tab. A query string would have hit the same path, since the comparison includes `search`.
  - **Copy link.** A button beside the list title once a build exists, `navigator.clipboard.writeText`, "copied" for two seconds; where the clipboard is refused, `window.prompt` holding the URL, which is ugly and works everywhere. Not `navigator.share`: desktop Chrome has it too and would open a share sheet under a button that says copy.

  **Done when:** a reload restores the build; a pasted link opens it in another browser in either locale; a hand-edited link with a VZ kit on `ma` opens as the VZ kit with no swaps; a garbage hash opens the empty car; the prerendered `/` still renders the empty car with no build in it.

  **Landed 2026-09-16**, checked against the generated site in Chrome: a cold load of a link restores the kit and both swaps; reverting a swap rewrites the hash without adding a history entry; reload restores it; the English locale link carries it across; an MA link naming the VZ kit 18095 opened as that kit with its motor swap cleared and the hash normalised to match; a garbage hash put in by hand was overwritten with the build's own link; copy link wrote the full URL and flipped to "copied" for two seconds. Review caught the one path the first test order missed: because the router never sees a `replaceState`, `switchLocalePath` copied the hash the page was *opened* with, so edit-then-switch-locale reverted the edits. The locale link now drops the hash — which is also right for guides, where an anchor is a slug of translated text — and the builder writes the current build back on arrival. What it does not do: tell the reader a link was trimmed. A part that no longer fits is dropped silently, which is right for a catalog correction and wrong the day a reader wonders where their roller went — that is a rule-engine note, not a link concern.
- Part and chassis pages, prerendered per locale, JSON-LD `Product`, sitemap, attribution page. Last rather than first: they are additive, they get cheaper once the builder has settled what a part row must show, and `PartCard` is already the component they will reuse.

  **Part pages scoped 2026-09-16.** `/parts/:id` and `/en/parts/:id`, served by `app/pages/parts/[id].vue`, which out-ranks the `[...slug].vue` catch-all — `/parts` itself stays the prose page. Chassis and kit pages are **not** in this chunk: each needs its own answer to what the page is for, while the part page is the one the builder already wants to link into.

  Counted before scoping, because it decides what the page can honestly have: a photo on 382 of 382 parts, a Tamiya link on 382, a tamiya.hk link on 304, a Fandom article on 121 (32%), and a "comes in these kits" list on **7**. The last is dropped rather than built. A loadout entry is a bare label with no `partId` for almost everything in a box (§4.7), so a reverse index over `stockLoadout` finds seven parts and reads as broken for the other 375.

  Four decisions, owner, 2026-09-16:

  - **The Fandom parts dump stops being QA-only, for one field.** `data/raw/fandom-parts.json` was a crossref source and nothing more (CLAUDE.md, §3.3): our categories come from substring rules over Japanese names, and the wiki's hand-written ones are what catch a short keyword swallowing a longer word. It now also yields `fandomTitle` on a part, exactly as `fandom-kits.json` already yields `loadoutSourceTitle` on a kit, and for the same two reasons — the outbound link is the CC-BY-SA attribution, and no other source groups item numbers into families. That buys the wiki link on 121 parts and a **variants** section on the 45 families that hold more than one of ours (largest: Aluminum Ball-Race Roller, 13; Hyper-Dash Motor, 10; 19 families hold exactly one and render nothing). Where two articles claim one item number the first wins, the rule `crossref.ts` already uses on the same file, and `catalog:report` prints every such clash rather than letting file order decide an attribution link in silence — none of our 382 is in that state today, two items elsewhere in the 474 the wiki lists are. `CLAUDE.md` says "nothing from it reaches `content/`" and must be amended in the same commit as the generator change, or the repo documents a rule it no longer follows.
  - **Add to build hands off to the builder; it does not resolve a slot.** A part page knows the part and nothing about the car, and the two namespaces only meet at `slotIdsFor(part, chassis)`. So the button writes a pending item number into state and navigates to `/`, where the whole catalog already lives: no build yet opens the base picker and applies the part once a base is chosen; one matching slot id swaps straight in; several (`roller-front`/`rear`/`side`, `wheel-front`/`rear`, `gear`/`counter-gear`) open a slot chooser; a part that does not fit the chassis lands without swapping and says so. The alternative — shipping the eight chassis slot profiles into all 764 part-page payloads to answer on the spot — buys a better button label for a second copy of the resolution logic. The resolver is pure and lives in `shared/catalog/build.ts` with the rest.
  - **A second thumbnail size, parts only.** 160×120 is right for a picker row and thin for a detail hero, and upscaling a 4 KB WebP looks worse than showing it small. `catalog:thumbs` gains a 320×240 pass over parts, written beside the existing file and falling back to it (**measured: 382 files, 5.0 KB average, 2.6 MB**, against an estimate of 4.5 MB). This stays inside the defence the 160s rest on (CLAUDE.md, §3.3): small, credited on the page, beside a link to Tamiya's own product page, one directory to delete.
  - **The browse index waits.** 764 pages that nothing links to are 764 pages Google will not find, but the fix is a real `/parts` — filters by category, chassis and slot, its own payload budget, its own argument about what a browse row shows — and that is a second feature, not a footnote to this one. Until then the routes are enumerated from `content/parts/` in `nuxt.config.ts` (a `readdirSync`, not a crawl, so a page cannot go missing because a link stopped rendering), and the inbound links come from the builder: `SlotRow`'s part text becomes a link, and `PartCard` gains an optional details affordance — a **sibling** of the picker's select button, never nested inside it.

  The page carries the photo, the resolved name with the same fallback marking the pickers use, the item number, the series badge (`GUP No.512` / AO / 限定), `¥` only (the HKD row comes off everywhere — Totals, above), status, and the add-to-build button; then the specs, chassis compatibility, class legality, the slots it fills, its names in the other locales — which is what someone searching a Japanese shop needs — the Tamiya, tamiya.hk and Fandom links, the variants, and up to eight other parts for the same slot, which is interlinking our own data pays for by itself. Two details the data forces: `specRowsFor(part)` with no slot returns weight alone, so the page needs a `specRowsForPart` that unions `BY_SLOT` over the part's own slot types; and the 53 parts with an empty `chassisCompat.include` must read *不限底盤*, never as a part that fits nothing, which is the one misreading the schema comment exists to prevent.

  **Never rendered:** `specsRaw` — Tamiya's own 【基本スペック】 text, which we store to parse and may not republish (§3.3) — `officialImage`, and `nameSources`, which belongs to the attribution page.

  JSON-LD carries `name`, `sku`, `brand`, `image` (our thumbnail, absolute) and `url`. **No `offers`**: we do not sell the part, and a price in an offer on a site with no checkout is a structured-data claim we cannot stand behind.

  Work, in order: `fandomTitle` through schema, generator, `catalog:verify` and `catalog:report`, with `CLAUDE.md` amended alongside; the 320×240 pass in `catalog:thumbs`; the route, the page, `specRowsForPart` and the `part.*` keys in both locale files, with the prerender list generated from the filesystem; the add-to-build handoff and its unit test; head tags and JSON-LD; the two inbound links.

  **Done when:** a cold load of `/parts/15512` and `/en/parts/15512` shows the part with every section it has data for and no empty ones; add-to-build from a cold part page opens the base picker and lands the part in the right slot; add-to-build with a build already in state swaps it in and the hash updates; a part that cannot go on the current chassis says so instead of silently doing nothing; `npm run generate` prerenders 382 × 2 new routes and the build time is measured and written down here.

  **Landed 2026-09-16**, driven in Chrome against the generated site: a cold part page hands its item number to the builder, which opens the base picker, and choosing an MA kit drops the brake into the brake slot with a notice naming it; the same button from a part reached *through* the builder finds three roller slots and asks which; the hash grows with each placement, so the share link carries a part added from its own page; no console errors anywhere in that run. Four things worth keeping:

  - **The detail size did not cost a single extra fetch.** The pass was scoped expecting to download 382 full-size photos, because a `_s.jpg` "thumbnail" sounded like something near 160 px. Measured, Tamiya's list-page photo is **400×300 or 600×450** — the row size has been downscaling from 2.5× all along — so the 320×240 copy comes from the same bytes already in `.cache`, and not one of the 382 sources was too small for the box. `thumbs.ts` skips a source narrower than the target rather than enlarging it, which is why `detailThumbnail` is optional and the page falls back to the row size; nothing exercises that path today.
  - **Column discipline is what makes 764 routes cheap.** A part page's payload is **1.4 KB gzipped** (15512) to **2.5 KB** (15437, whose wiki family runs to twelve links), against the builder's 82 KB. The related list is cut to eight *before* the return, so a 60-part category never reaches the wire; the chassis records are filtered to the ones the part fits; `specsRaw`, `officialImage` and `nameSources` are never selected. Whole records would have made each page a small copy of the builder.
  - **The whole build is still 7.4 s.** 1,555 routes prerendered in 3.05 s, `.output/public` 22 MB. Adding 1,528 routes (764 pages and their payloads) did not change the shape of CI.
  - **Link prefetch was checked and is not a cost here.** 764 pages each carrying a nav link plus up to twenty part links looks like an invitation for Nuxt to speculatively fetch `_payload.json` for every link that scrolls into view — which would mean ~50 KB of payload pulled around a 1.2 KB page. Measured in Chrome on the generated site, a part page with all twenty links scrolled through issues **one** payload request: its own. `experimental.appManifest: false` (already set, for its own reasons) is what switches that mechanism off, because the payload plugin will not prefetch a route it cannot confirm is prerendered. Worth knowing before anyone turns the manifest back on.
  - **Coverage came out where the count said it would**: `fandomTitle` on 121 of 382 parts, 45 families with more than one of ours, and zero item numbers claimed by two articles — the `catalog:report` line that guards that reads 0 today and is there for the day it does not.

  Still open, and deliberately: ~~`/parts` is the prose stub it was, so the only inbound links are from the builder's rows and pickers; there is no sitemap; and chassis~~ — all three closed by the next item, 2026-09-16 — and kit pages are untouched.

- Browse indexes, chassis pages and breadcrumbs, prerendered per locale, `BreadcrumbList` JSON-LD, sitemap. The chunk the part pages deferred — "the browse index waits… a second feature, not a footnote to this one", above — plus the trail that makes it reachable from below.

  **Scoped 2026-09-16.** `/parts` becomes a category grid, `/parts/category/<slug>` lists one category, `/chassis` and `/chassis/<id>` land alongside them, and every page grows a crumb trail. Kits are **not** in this chunk: a kit page needs its own answer to what the page is for, exactly as the part page did, and nothing here needs one.

  Counted before scoping, because the counts decide the shape: **29 of the 32 `PART_CATEGORIES` have members** — `switch`, `battery` and `battery-holder` have none and must not be prerendered — the largest five are roller 49, plate 46, wheel-tire-set 35, motor 32 and accessory 27, and five run to three parts or fewer (`motor-mount` 1, `tool` 2, `setting-tool` 2, `sticker` 3, `other` 3). Kits, for the chassis pages that list them, spread over all 8 chassis: ma 69, vs 50, super-2 50, ms 50, ar 34, fm-a 29, vz 21, me 2.

  **`category` partitions the catalog, and that is what makes it the indexed dimension.** It is a single-valued `z.enum` on every part, so the 29 pages divide 382 parts with no part appearing on two of them — 29 listing pages and not one near-duplicate. A chassis facet does not partition: a part is listed by up to eight chassis, so `/parts/chassis/ma` and `/parts/chassis/vz` would be two long lists mostly of the same rows. That is the reason the chassis level below is a page *about* the chassis rather than a second listing of its compatible parts, and the reason `/parts/slot/<slot>` and `/parts/series/<gup|ao|limited>` are dropped rather than built: three near-copies of the roller category, and a cut across every category, respectively.

  Six decisions, owner, 2026-09-16:

  - **The detail URLs do not move.** `/parts/15512` does not become `/parts/roller/15512`. GitHub Pages cannot issue a 301, so 764 prerendered, already-shareable URLs would break with no server-side way to forward them, and the share links from the hash work above name those paths. A part has one category but several slots and up to eight compatible chassis, so any hierarchical path picks one parent arbitrarily. None of that costs us the hierarchy, because **`BreadcrumbList` does not have to mirror the URL** — the trail is declared in JSON-LD and the paths stay flat. Flat URLs, hierarchical crumbs.
  - **Thin categories render anyway.** `motor-mount` holds one part. The alternative — folding anything under three into `other` — was rejected: the taxonomy node is real, the crumb on that part's page needs somewhere to point, and a category that grows past the threshold later would silently change its own URL. They are small pages, not wrong ones.
  - **Chassis pages come into this chunk, not after it.** Eight records, almost all of it data some page already renders, and they are what makes the link graph connected: the part page's compatibility chips become links, which is ~1,900 new internal links pointing at eight pages, and the eight then point back into the categories. Building the parts level alone would leave the chassis names on 764 pages as dead text.
  - **Category slugs stay English in both locales.** `/parts/category/wheel-tire-set` and `/en/parts/category/wheel-tire-set`. Translating a dynamic segment needs a per-locale lookup table in `customRoutes`, and a Traditional Chinese slug is percent-encoded mush the moment it is pasted into a chat — the same argument that keeps item numbers in the part URLs. The displayed label is `part.category.*`, already in both locale files, read through `term()` so 摩打 and 剎車 follow the wording toggle like every other term on the site.
  - **A filter inside a category lives in the hash, never in a query string.** A chassis chip row on `/parts/category/roller` narrows 49 rows to the ones that fit an MA. As `?chassis=ma` that is a crawlable near-duplicate of the page, and a static site has no headers to `noindex` it with; as state alone it does not survive a reload or a paste. `#ma` is neither: hashes are never indexed as separate URLs, and they share and reload. **`useBuildLink`'s lesson applies verbatim** — watch `route.hash`, never read `location.hash` in `onMounted`, because a prerendered page hydrates with the hash stripped and restores it after mount (above).
  - **The sitemap lands here.** It has been listed as missing since the part pages shipped, and it is the same story as the rest of this chunk: routes that exist and nothing points at. `@nuxtjs/sitemap` reads the prerendered route list and emits the per-locale alternates §4.6 asks for, which is a dependency against ~40 lines of postbuild script over the route list `nuxt.config.ts` already computes. Take the module.

  What each page carries, with the part page's column discipline, which is what made 764 routes cost 1.4–2.5 KB gz each:

  - **`/parts`** — 29 cards, each an icon from `app/utils/icons.ts`, the translated label and a count, grouped from a `select('category')` over the parts. Deliberately **not** a 382-row list: the browse payload stays near zero, and search over everything already exists in the builder's picker. Do not build a second one.
  - **`/parts/category/<slug>`** — its own rows only, reusing `PartLinkList` and the part page's `STUB` columns (`id`, `stem`, `names`, `thumbnail`), plus the chassis chip row. Biggest is 49 rows against the part page's measured 2.5 KB gz for twenty links, so **expect ~4–5 KB gz and write down what it actually is.**
  - **`/chassis/<id>`** — the specs (wheelbase, tread, weight, kit gear ratio, motor position and shaft, release year), our own `notes` prose, the thumbnail, the slot profile, the `defaultLoadout` a bare runner gives you, and the kits that use it. The kit rows have nowhere to link yet, so each one links **into the builder** as an encoded build (`shared/catalog/share.ts` already encodes chassis + kit), which answers the question a beginner actually brings to a chassis page: which box on the shelf contains this. Google ignores the hash, so this is an affordance and not link equity; say so rather than counting it. `compatibleParts` is a count per category linking to the category pages, not a list — MA's runs to the hundreds.
  - **`/chassis`** — eight cards. Small enough that a grid of thumbnails and one spec line each is the whole page.

  **The crumb is one component.** `app/components/Breadcrumbs.vue` takes a trail, renders `<nav aria-label>` + `<ol>` with the last item as `aria-current="page"` text rather than a link, and emits the `BreadcrumbList` JSON-LD itself with absolute `siteUrl + localePath(…)` URLs — the pattern `parts/[id].vue` already uses for `Product`, and two `application/ld+json` blocks on one page is fine. The part page passes four levels (首頁 › 零件 › 滾輪 › the part), the category page three, and `[...slug].vue` derives its trail from the route path, which gives guides and about one for free. The payoff is not the SERP display: **it is the only upward link in the site.** The graph today points down (builder → part) and sideways (part → variants → related) and never up, so the 764 pages hang off one payload-heavy home page.

  Two files must die in the same commit that adds `app/pages/parts/index.vue`: `content/zh-Hant/parts/index.md` and `content/en/parts/index.md`. The page component out-ranks the `[...slug].vue` catch-all, so leaving them ships dead content that no route can reach.

  Prerendering follows the rule the part routes already set — enumerate from the filesystem, do not trust `crawlLinks`, so a page cannot go missing because a link stopped rendering. The categories cannot come from `PART_CATEGORIES`, which would emit three empty pages, so `nuxt.config.ts` reads each `content/parts/*.yml` and matches `^category: (.+)$`; 382 small reads at config time, no YAML parser, same spirit as the existing `readdirSync`.

  Work, in order: `Breadcrumbs.vue` with its JSON-LD and the `breadcrumb.*` keys in both locale files, wired into the part page and the catch-all first, because it is useful before any index exists; `/parts` and the deletion of the two stubs; `/parts/category/[slug].vue` and the route enumeration; the in-category chassis chips on `route.hash`; `/chassis` and `/chassis/[id].vue`, with the part page's compatibility chips becoming links and its 相關零件 heading becoming a link to the category ("see all 49"); the sitemap.

  **Done when:** a cold `/parts` and `/en/parts` show 29 categories with counts that match the YAML; `/parts/category/roller` lists all 49 and its payload is measured and written here; `/parts/category/roller#ma` opens filtered and survives a reload; every part page shows a four-level trail whose category link resolves in both locales; `/chassis/ma` lists its 69 kits and each one opens the builder on that kit; `npm run generate` prerenders the new routes (29 + 8 + 2 index levels, ×2 locales) with the build time recorded here against the current 7.4 s; Google's Rich Results Test validates both `Product` and `BreadcrumbList` on one part page and `BreadcrumbList` on one category page.

  **Landed 2026-09-16**, driven in Chrome against the generated site: `/parts` opens on 29 cards whose counts match the YAML, `/parts/category/roller` lists all 49 with a chassis chip row, tapping ME narrows it to 35 and writes `#me`, a cold reload of that URL comes back filtered, `/chassis/ma` carries specs, the runner's twelve filled slots, 25 category chips and 69 kits, and clicking the first kit opens the builder on kit 17901 with its body and gear ratio in place. No console errors anywhere in that run. Seven things worth keeping:

  - **The measurements came out where the counts said.** A category payload is **3,995 B gzipped** at its largest (roller, 49 rows) against the 4–5 KB the scope predicted; `/parts` is **641 B** and `/chassis` **674 B**, which is what not shipping a 382-row list buys. A chassis page is **6,053 B** (MA, 69 kits) down to 4,539 B (VS, 50) and 3,290 B (VZ, 21), after the loader pass under *Review found* below. The part page did not move: 1,385 B for 15512, where it was 1,384 — `categoryTotal` is one integer and the compatibility chips became links without adding a field.
  - **The whole build is 9.7 s.** 1,709 routes prerendered in 4.5 s, `.output/public` 27 MB. 154 routes added (76 pages, 76 payloads, `sitemap.xml`, and `/parts` becoming a real page) for 1.4 s of prerender. CI's shape is unchanged.
  - **Importing a *value* from `schema.ts` ships Zod to the reader, and review is what caught it.** Two of the new pages wanted `CHASSIS_IDS` — eight strings, to sort a chip row and validate a hash — and importing the const pulled **60 KB of validator** into `/parts/category/*` and `/chassis`, which no other route on the site carries. `share.ts` had already written the warning down ("the tables live in `share.ts` rather than being imported from `schema.ts`, which would put Zod in the page") and it was still walked into, because the import looks free. The fix is a module that makes it free: `shared/catalog/chassis.ts` holds `CHASSIS_IDS`, `isChassisId` and `byChassisOrder` with no Zod in it, and `schema.ts` imports and re-exports them so every existing caller is unchanged. Measured after: the category route's JavaScript went 386 KB to 326 KB and no route loads the Zod chunk. **Grep the built chunks for `ZodError` before believing a page is small.**
  - **A sitemap nothing points at is one only Search Console knows about.** The module writes the file and stops there; `public/robots.txt` — which this site did not have — is the one place a crawler looks for it unprompted, so it landed with it.
  - **The sitemap is exactly the page count, and that is the check worth keeping.** 848 URLs = (382 parts + 29 categories + 8 chassis + `/`, `/parts`, `/chassis`, `/about`, `/guides`) × 2 locales, with `xhtml:link` alternates and an `x-default` on every entry and `/build` excluded. If that number ever stops being `pages × 2`, something is either orphaned or double-listed.
  - **Two `application/ld+json` blocks on one page need a key.** A part page now carries `Product` and `BreadcrumbList`, and unhead is entitled to collapse two script tags that differ only in their body. `Breadcrumbs.vue` passes `key: 'breadcrumbs'`; both blocks are in the prerendered HTML and parse.
  - **`route.hash` again, and the same trap.** The chip row reads the filter from a watcher on `route.hash` inside `onMounted`, never `location.hash`, for the reason `useBuildLink` documents — a prerendered page hydrates with the hash stripped and has it restored on `app:suspense:resolve`. A cold load of `/parts/category/roller#me` renders 49 rows in the HTML and 35 after hydration, which is the right order: the unfiltered page is the one at that URL.
  - **The English category labels were lowercase, and that was only ever right for one caller.** They were written for `part.description`'s mid-sentence "Mini 4WD roller part", and as an `<h1>`, a crumb and a card they read as a typo. Sentence-cased, with `part.description` and `browse.category.description` reworded around the capital. `motor-mount` also joined the `terms` dictionary: the category label is a heading now, and it inherits the 摩打/馬達 split that `motor` already followed.
  - **Chassis pages made `thumbs.ts` tell the truth again.** Its detail pass was commented "parts only: they are the only records with a page of their own", which stopped being true the moment `/chassis/:id` existed — a 160×120 photo stretched to a 320 px hero is the upscale that comment exists to prevent. The pass now covers chassis: **8 written, 6.6 KB average**, every source big enough, `detailThumbnail` added to `chassisSchema`, the generator and `catalog:verify` beside the part field it copies.

  Review found two things measurement would not have, and one it did:

  - **The sitemap was submitting 8,592 Tamiya photos to Google Images.** `@nuxtjs/sitemap` scans the prerendered HTML for `<img>` by default and lifts every one into an `image:image` entry — 1.1 MB of the 1.4 MB file. The bytes are the small half. Those images are our downscales of Tamiya's photos, and the defence they rest on is that each is small, credited on the page, and beside a link to Tamiya's own product page (§3.3); an image sitemap asks Google to index them *stripped of all three*. `discoverImages: false`, and the file is 319 KB raw / 11.8 KB gz.
  - **`names` ships all four locales on every row, and the page's locale is fixed at prerender time.** `useCatalogName` pins the locale per route and only the hk/tw wording varied in the browser (and since 2026-09-16 not even that, §4.1), so an English payload carries `zh-HK` and `zh-TW` that `ORDER.en` can never reach. Measured by rewriting the payloads: `/en/parts/category/roller` **−1,717 B gz (−44%)**, `/parts/category/roller` −1,369 B (−35%), `/en/chassis/ma` −1,895 B (−32%), `/parts/15512` −333 B (−25%). **Not done here**, and the reason is the reason it is worth doing: it is not a property of these pages. It changes what every loader returns, what `PartLinkList` reads for its fallback mark, and the payload of all 764 part pages and the builder — a pass over the catalog access layer, with its own before/after table. Scoped as its own item rather than smuggled in beside a browse index.
  - **What was fixed here**: the chassis loader's three queries now start together rather than fetching the record first; the all-parts tally query dropped `names` (36 KB read to use at most one row); `slots[].mirror`, `slots[].required` and a loadout entry's `source` are stripped before the return; `notes` is dropped on English routes in the *loader* rather than hidden in a computed, which is why the key is now `chassis-<locale>-<id>` — one key for both locales would hand whichever payload arrived first to the other on a client-side switch. The 69 kit links are built once into a `Map` instead of re-packing a build per row per render, and a loadout entry resolves its name once instead of twice.

  Still open, and deliberately: `chassis.notes` is a single string and every value in it is Traditional Chinese, so an English chassis page carries the specs, the loadout and the kits but no prose — localising it is a schema change and eight hand-written paragraphs, not a footnote to this chunk. Kit pages still do not exist, which is why a kit row on a chassis page opens the builder instead of a page; that is the affordance the question deserves, but it is not a kit page. And `/parts` has no search of its own — the builder's picker is still the only one. One direction to watch rather than fix: `/chassis/ma`'s payload grows linearly with its kit count, which only goes up — 69 of 305 kits are MA today, and nothing caps that list.

- ~~**Nav and locale menu.**~~ — done 2026-09-16 (`4514d50`). The bar asked two questions side by side, the other locale and 用語 港／台, and the data can only answer the first. The wording toggle is withdrawn (§4.1): `useWording` returns `hk` as a constant, so `useTerm` and `useCatalogName` did not change, and the `tw` data and zh-TW hreflang stay. One `<details>` menu now lists 繁體中文 and English, because its links have to be in the prerendered HTML for a crawler to reach `/en`. The bar gained the logo; 首頁 became 組車, because `/` is the builder and "home" does not say what a reader gets there; and 指南 is a disabled span until `/guides` is more than the scaffolded stub (the route stays prerendered and in the sitemap). Breadcrumbs still start at 首頁／Home. Cost: +76 B gzipped of nav markup, the payload unchanged, and one less synchronous `localStorage` read on hydration.
- ~~**Base row and body picture.**~~ — done 2026-09-16 (`7ca067d`). The 套件／底盤 row now shows the **chassis** thumbnail, which answers what the row is for: which chassis is in the box. The box art moved to 車殼, the slot it actually shows. A kit's moulded body was never sold as a part, so that slot had no photo and drew the slot icon. `stockThumbFor` in `app/pages/index.vue` does this for the body slot only, and `SlotRow` uses it only while the entry's `origin` is `kit`. Swap in a real body part and its own photo shows; Revert brings the box art back. This is the first UI that reads `origin` since the badge went. No payload cost, because thumbnail paths come from item numbers.
- ~~**Stock propeller shaft as a real part.**~~ — done 2026-09-16 (`2f6d657`). The five single-shaft chassis now name the shaft as an AO spare instead of a label, the same way AO-1001 (94380) is their stock motor: FM-A takes AO-1044, which Tamiya tags for it, and VS, AR, Super-II and VZ take AO-1004 (94382). Tamiya tags AO-1004 for VS only; the other three are an **inference** from 15205, the hollow shaft Tamiya lists for all four, and `data/overrides/parts.yml` says so. The override was needed because `catalog:verify` checks a loadout part against the slot type, not the chassis, so the stock shaft would have passed verify while `isChassisCompatible` kept it out of that chassis' own picker: shown until swapped, then never offered again. `generate.ts` now sets `chassisCompat.source: override` when an override changes compatibility, as it already did for category and class legality. The chassis page loads its loadout parts in one `stem IN (…)` query and skips it on the three PRO chassis, which name no parts. Still true: MA, MS and ME have no stock motor part, because Tamiya sells no double-shaft normal motor.

**c. 3D view and selection (§5.4).** ~~MA chassis with slot-named sockets~~ all eight chassis from the primitive helper, each with its socket table, MA first and the rest by kit count (owner, 2026-09-16, §5.6), ~~3 generic bodies~~ per-kit body silhouettes from one loft and a shape table, ~12 parametric generators — **all scripted in three.js in the browser, not modelled in Blender or exported from build123d** (owner, 2026-09-16, §5.6) — the fallback proxy, ~~`@tresjs/nuxt`~~ plain three (owner, 2026-09-16, §4.1) ~~on the `/build` route only~~ on the builder, which is now `/`, client-only and code-split. **First chunk, scoped 2026-09-16: the pane with no meshes (§5.5)** — proxy boxes at code-built sockets named by slot id, tap-to-open the existing picker, three loaded only once a build exists. It answers the three unknowns (Nuxt SSR, chunk size, phone tappability) before any Blender hour is spent. **Landed 2026-09-16** — 137 KB gz fetched only once a build exists, near-side targets over 44 px on a phone-sized frame, the far side an orbit away; what it changed is under §5.5 *Built and measured*. ~~**Last chunk: the hero.** Once MA and the visible-slot generators render a recognisable car, the landing page shows a default MA build through the same scene component and model-viewer is removed (§5.5).~~ **Overtaken 2026-09-16:** the builder became the home page, so the hero *is* the builder's pane — an empty MA proxy car behind a poster image until it draws — and model-viewer and `4wd.glb` went with the old landing page, before any mesh existed. Orbit, zoom, reset camera — **and click the car to pick a slot**, which pulls §5.2's raycast proxy volumes, hover highlight and mobile touch tuning into M1 (owner's decision, 2026-09-09). ~~Other chassis show a still and a notice, and are built through the list.~~ Until its generator lands, a chassis shows the placeholder and is built through the list; the seven are M1 work since 2026-09-16 (§5.6), 20–30 h. **Wheels and tires, scoped and landed 2026-09-18 (§5.6):** the generators take a shape per Tamiya wheel and tire type — 46 wheels and 28 tires, reached by all 305 kits and all 53 wheel and tire parts — instead of one rim at one size. +1.6 KB gz of 3D chunk, +1.1 KB of payload, and `tireDiameterMm` went from 0 parts to 53, which took tire diameter off §4.3's unchecked list.

This is the milestone's long pole and the one most likely to slip. The fallback that protects the date is the 2D builder, which is already built and already works on all eight chassis: if the meshes or the raycast work run over, M1 ships list-first with the 3D pane view-only, and selection moves into the canvas when it is ready.

**M1 is done when:** a beginner can open ~~`/build`~~ `/`, pick the kit box on their desk, change the motor and rollers, ~~see a warning that the motor is Open-only~~ see a warning when the motor is not allowed in the class they picked (reworded 2026-09-16: no part is Open-only in the data, see the rule engine above), ~~see the cost of what they still need to buy,~~ watch the car update in 3D, and send the link to a friend — in Traditional Chinese and en. (Cost clause removed 2026-09-16 by the owner; see Totals above.)

**Run end to end on 2026-09-17, in Chrome with phone emulation (390×844 and 360×780, touch), against the generated site — and it passes.** A cold `/` opens on the empty car; the kit picker finds 17901 by item number; a tap on the car opens the motor picker, and a Hyper-Dash PRO and a pair of 15529 front rollers go on, both turning orange in the pane; Open and Stock Class report nothing, Junior reports one error on the motor, and switching class leaves the hash alone; a reload restores the build; copy link writes the URL and says so; the link opened in a fresh browser context on `/en` at 360 px shows both swaps with Revert, checks against that reader's own class (Open), has no horizontal scroll and logs nothing to the console. What emulation cannot say is how a thumb feels, so **a real phone is still owed**; everything below is measured with synthetic touch taps sent every 6 px across the pane (8 px at 360), recording which picker each one opened.

| Slot, MA kit at 390 px | Shell seated | Shell lifted |
|---|---|---|
| Front roller | 60×60 | 60×60 |
| Front stay | 84×60 | 84×60 |
| Side roller, damper, front tire | 42–48 | 42–48 |
| Front wheel | 30×42 | 30×42 |
| Motor | covered | 66×18 → **66×48** after the fix below |
| Rear tire, rear wheel, brake, rear stay | 6–24 | 18–30 |

VS at 360 px has the same shape: the transverse motor is 56×48 lifted, the near side comfortable, the far side small. Three things worth keeping:

- **A lifted shell still took the taps it was lifted to uncover.** It fades to 35% so it hides nothing, but it stayed the nearest raycast hit, and raised it covers *more* of the pane (216×84) than seated, leaving the motor an 18 px strip. While lifted, any other part under the finger now wins and the shell takes a tap only where nothing else is (`proxyAt` in `Scene.client.vue`); seated behaviour is unchanged. Motor 25 → 68 grid cells, brake and rear stay roughly doubled, and the shell keeps a 216×72 area to seat it by.
- **The far side is still an orbit away**, as §5.5 accepted: rear wheel, rear tire, brake and rear stay are under 30 px from the home view. Not changed.
- **The roller picker opens on accessories.** `partsForSlot` ranks by legality, then price, and the `roller` category holds O-rings, 5 mm spacers and roller mounts beside the rollers, so the cheapest rows — four of the first six — are not rollers. A beginner who tapped a roller to change it sees rubber rings first. ~~Not fixed here: it is a ranking or taxonomy decision (plates share their slot with slide dampers the same way), not a tap bug.~~ **Fixed 2026-09-18, and it was two bugs, not one** — see below.

**Pickers list the thing the slot is named for first (2026-09-18).** The rest of the M1 run was fine; this one sat on the done-criterion's own path ("change the motor and rollers"). Counted over every slot before touching anything, the rows at the top of four pickers were not what the slot is named for, and for two different reasons:

- **25 parts are in the right category but are add-ons, not the thing itself.** Roller O-rings, pipes, spacers and roller bearings, and the wide mount (9 in `roller`); single AO gears, pinions, gear shafts and the MS gear bearings (11 in `gear`); two slide-damper spring sets; two stabilizer ball caps. They are cheap, so ranking by price put them on top: on MA, 4 of the first 6 roller rows, the first 2 stay rows, the first 4 damper rows and all of the first 6 gear rows — 10 of the gear picker's 15 rows are spares. Each category rule in `data/taxonomy/categories.yml` can now carry an `addOn` pattern, tested only against parts that rule has already claimed. That matters: `ローラー用` ("for rollers") is on an O-ring and on two FRP plates alike, and only the O-ring is a roller's add-on. The generator writes `isAddOn: true` on those 25. `partsForSlot` ranks add-ons below every other part, including illegal ones, so that they form one block at the foot of the list; `PartPicker` puts a 搭配及補充零件 / "Add-ons and spares" divider above that block. They are ranked down, not filtered out: they do fill the slot, a link may carry one, and a search by item number still finds them. `catalog:verify` fails if any slot on any chassis offers only add-ons, so a pattern that is too broad breaks CI before it breaks a picker.
- **4 parts were in the wrong category**, and three of them from the failure `categories.yml` already warns about with `シール`: the plate rule's `ステー` also matches `ステーション`, so two Mini 4WD Station editions — a 19 mm roller (95697) and a tube stabilizer (95718) — were plates. The rule is now `ステー(?!ション)`. The other two are the ball-link mass dampers sold with a carbon plate (95387, 95180), which `プレート` claimed; `mass-damper` now sits above `plate`, which moves nothing else, since no plate name contains its keywords. `plate` goes from 46 to 42 parts, `roller` from 49 to 50. These four change slots, so a link carrying one in its old slot opens trimmed. Analytics started today, so hardly any such links can exist.

After the change, the first six rows of the front roller, front stay, damper and gear pickers on MA and VS contain no add-on, which was checked in Chrome against the generated site. The motor picker, which has no add-ons, has no divider. Payload cost: **+146 B gzipped** on the builder (90,441 → 90,587; +4.2 KB raw), because @nuxt/content returns `null` for an absent column. Writing `isAddOn` only when true saves nothing in the payload, the same as `finish`, and keeps the YAML diff to 25 lines. The roller category page grew 100 B, for the one roller that joined it. No route loads Zod.

Found and **not** fixed here, because fixing them changes wheel shapes that landed the same day (§5.6): three more misfiles of the same kind in the wheel family. 95680 and 95602 are aluminium wheels *for* low-profile or narrow tires (`…タイヤ用アルミホイール`), and 15507 is a sponge tire *for* large narrow wheels (`…タイヤ（…ホイール用）`). The `wheel-tire-set` regex reads each of them as a set, so the wheels appear in the tire picker and the tire in the wheel picker. **Fixed 2026-09-18:** 用 ("for") marks the word that only says what a part fits, so the set rule now declines a name where ホイール or タイヤ is followed by 用 (a bracketed size may sit between, as in `ナロータイヤ (24mm) 用`), and `wheel` moved above `tire` so a wheel for a named tire does not fall through to the タイヤ substring. Those three parts, and nothing else, change category; the wheels keep their shapes and lose the tire the set drew. 15507's colour override existed only to paint the misfiled set all black and is gone. On the way, every plain wheel now records the diameter of the tire it fits, which `app/utils/specs.ts` already said it held and the generator never wrote: 8 wheels, all ⌀24 or ⌀26, and `catalog:verify` holds a wheel to the millimetres its name prints as it does a tire.

**Future work, not scoped (2026-09-18):**
- **Gear positions as sub-slots.** A gear set fills the gear slot on its own; a single AO gear or a pinion is a spare for one position. Holding several parts in one gear slot would say nothing about whether the gear train is complete or what ratio it gives. The model that can answer both is one sub-slot per position (pinion, counter gear, crown or spur), where a set fills all of them. `slots.yml`'s `counter-gear` comment is already half of that. It needs the same data as the gear-ratio total above: which positions each gear part covers, and tooth counts. 8 of 22 gear parts carry a ratio (3 until 2026-09-18).
- **An "add another" picker for stacking slots.** `damper` (4), `brake` (2) and `fastener` (8) hold several different parts on a real car, and the build state, the link format and `SlotRow` all carry a list already. Only the picker replaces with one part. It needs an add-versus-replace action, and the pane has to place several parts in one socket.

**Explicitly not in M1:** the wizard, votes, accounts, short links, OG images, D1, ~~per-kit body shapes~~ (in M1 since 2026-09-16 as silhouettes, §5.6; per-kit *meshes* stay out), ~~chassis other than MA in 3D~~ (in M1 since 2026-09-16, §5.6), build cost totals. Still on GitHub Pages, still fully static. (Click-to-select in 3D was on this list until 2026-09-09 and is now M1c.)

### M2 — Permalinks, sharing, community signals (4–6 weeks)

Move to Cloudflare Workers with static assets; DNS to Cloudflare; D1 and R2. Short links and `/b/:id`, canvas-snapshot thumbnails to R2, `nuxt-og-image` share cards, Turnstile, rate limiting, server-side re-validation of shared builds. Anonymous votes on builds and parts, view counts, "popular builds" and "most used parts". Guides and tutorials (F2): class glossary, chassis picker, motor guide, first-upgrade guide, setup basics.

### M3 — Full 3D builder (6–10 weeks)

~~The remaining seven chassis,~~ (M1c since 2026-09-16, §5.6) ~~per-kit bodies where they are worth modelling~~ ~~the silhouette table for the kits outside MA~~ (M1c since 2026-09-18, §5.6), and the rest of the parametric library — which is what actually lets the 2D list stop being the primary surface on anything but MA. Swap animation and camera presets (not the switch-on spin, which is M1c since 2026-09-18, §5.6: that turns the wheels and gears in place, a swap animation moves a part into its socket). 3D preview on share pages; AR, if wanted, by bringing model-viewer back on share pages only, against a baked export (it leaves the site in M1c, §5.5). The raycast proxies, click-to-select, hover highlight and touch tuning that used to live here moved to M1c on 2026-09-09.

### M4 — Wizard, identity, community (ongoing)

Beginner wizard (F5) seeded from the six archetypes in §2.5 and weighted by the popularity data M2 collects — it is a better recommender with real usage behind it, which is why it moved after M2 rather than before. Optional login, garages, comments, `ja` locale, contribution workflow, regional pricing and build cost totals (out of M1 since 2026-09-16, §6 M1b), price history, per-course setting notes.

---

## 7. Risks and open questions

**Resolved:** *What "B-Stock" means locally* — the site owner confirmed on 2026-09-08 that B-Stock is local shorthand for **Stock Class**. It is modelled as an alias of Stock Class (and thus B-MAX GP), not as a separate ruleset. See §2.1.

| Risk / question | Mitigation |
|---|---|
| Tamiya IP: names, photos, 3D shapes | Facts + own text, own photos for builder parts, stylised 3D, disclaimer, no GLB downloads |
| Selection moved into the 3D canvas (owner, 2026-09-09), so M1 now depends on the milestone's riskiest workstream | The 2D list is already built and covers all eight chassis, so it is a real fallback rather than a promised one: M1 can ship list-first with a view-only pane. What cannot be dropped is the list itself, and not only because one chassis is modelled and seven are not. All three of its jobs are permanent: keyboard and screen-reader users, anyone who knows the item number they want, and anyone who simply does not care to pick parts off a 3D car. The mesh-coverage argument is the temporary one |
| ~~Hotlinking Tamiya's CDN for product images (owner, 2026-09-09) puts a dependency we do not control on the critical path of the picker~~ **Superseded 2026-09-16: we self-host downscaled thumbnails, which makes us the publisher of the copies.** Fair use is weak in Hong Kong and Japan (neither has a general doctrine; HK's fair dealing is a closed list) and arguable in the US for thumbnails (*Kelly*, *Perfect 10*) but not settled for a catalog. Unlike Fandom, which hosts the same photos under `{{Fairuse}}`, we have no §512 safe harbour: their uploads are users', ours are ours | The realistic outcome is a takedown request, not damages, and everything is arranged so that complying is cheap and total: 160×120 only, never the original; credited on every page that shows them (`build.imageCredit`) and disclaiming affiliation; confined to `public/thumbs/`, so honouring a notice is deleting a directory and re-running `catalog:generate`; and `CatalogThumb` already falls back to a drawn slot icon, so every row still reads with no pictures at all. The `officialImage` URL stays a stored fact, so the originals can always be found again. Own photos still replace them for builder parts as they are taken. Worth doing before traffic grows: publish a takedown contact, and ask Tamiya or tamiya.hk for permission — a "yes" turns tolerance into a licence and a "no" leaves us where a block would have |
| 3D asset effort (150–190 h) dominates the timeline | M1 buys only ~40–50 h of it: one chassis, ~~3 generic bodies~~ one body loft and MA's silhouette table (§5.6), ~12 parametric generators, and a labelled fallback proxy so the builder ships independently of mesh coverage (§5.4) |
| Pulling 3D into the MVP repeats the mistake §1 warned about | ~~The 3D **view** is cheap and bounded; the 3D **input** surface (raycast proxies, click-to-select, touch tuning) is the expensive half and stays in M3.~~ Both halves are in M1 as of 2026-09-09, so this mitigation is now only the second sentence: if the MA chassis mesh slips, the fallback proxy means M1 still ships with a recognisable car, and the 2D list means it still ships with a working builder. That is a weaker position than the plan held yesterday, and it is deliberate |
| ~~Tamiya publishes no bill-of-materials for kits, so "start from my kit" has no source~~ | Resolved 2026-09-08: 305 kits committed, 96% with a `stockLoadout` imported from the wiki's `Technical Info List`, the chassis `defaultLoadout` supplying the rest, and the 13 uncovered kits degrading to it rather than breaking (§4.7) |
| Wiki kit data is CC-BY-SA and free text, not item numbers | Each kit carries `loadoutSourceTitle` so kit pages attribute on the page itself, not only the attribution page. Free-text values are deliberately **not** forced into item numbers: a loadout entry may carry a label alone, which is correct because most moulded parts were never sold separately. `catalog:report` ranks those labels by frequency so `data/overrides/kits.yml` can name the few that do map. Tamiya stays canonical on any fact the two disagree on |
| Body shapes are the highest IP risk and the priciest geometry | ~~M1 ships 3 generic stylised archetypes shared across kits, labelled in the UI as representative, not per-kit reproductions (§5.4)~~ M1 ships per-kit silhouettes: a dozen numbers per body through one loft, no surface detail or decals, shared across recolours, labelled as representative (§5.6) |
| Image-to-3D quality for thin parts | ~~Only used for body blockouts; procedural/Blender for everything else~~ Not used: parts and bodies are generated in the browser from specs, Blender is reserved for the chassis if primitives fall short (§5.6) |
| Cloudflare free-tier limits (KV 1K writes/day, D1 hard limits since 2026-09-01) | Use D1 not KV for writes; prerender everything; Workers Paid is $5/mo if ever needed |
| Scraper brittleness (Tamiya site is classic ASP, Shift_JIS) | One-off scrape committed to git; monthly diff run; manual review of new items |
| TC naming has no source of truth (HK 摩打 vs TW 馬達) | Store HK and TW variants; glossary page explaining both. The site reads in Hong Kong wording until there are Taiwan names to switch to (§4.1) |
| Loadout labels are single-language free text, and the builder now renders them. A chassis' `defaultLoadout` labels are hand-authored Traditional Chinese (`套件標準雙軸馬達`), so the English builder shows Chinese; kit `stockLoadout` labels are imported English from the wiki (`Small Low-Profile Slick`, 268×), so the Chinese builder will show English the moment the kit entry point lands | Surfaced 2026-09-09 by the builder spine, **scoped the same day — see §4.8**, and it is smaller than it looked. `label` is four jobs in one field: 615 of 1,783 kit entries restate `names` or `gearRatio` and need no translation, six motor labels are catalog parts that belong in `data/overrides/kits.yml`, and only ~40 normalised wheel and tire phrases are a real translation. Still its own chunk, still **before** the kit entry point rather than after |
| `chassis.notes` is one hand-authored Traditional Chinese string with no locale variants | Hidden on `/en` rather than shown in the wrong language (`ChassisPicker.vue`). Localise the field when the loadout labels are localised — it is the same schema problem |
| Catalog payloads are duplicated per locale. `/build/_payload.json` and `/en/build/_payload.json` are **byte-identical** at 185 KB (24 KB brotli), because every record carries all four locale names inline (§4.6) and the prerender ships the whole catalog to each locale tree | Noted 2026-09-09, not acted on. The cost is one extra copy per locale and it grows linearly as `ja` and any future locale land — cheap to fix at two locales, annoying at three. Not a blocker for M1: the absolute number is small and the alternative (splitting names out of the record, or querying client-side) is worse on both payload and complexity today |
| Rule coverage can never be 100% | Three severities; "verify with organiser PDF" notes; rules are data so community can PR fixes |
| ~~Existing scaffold is on EOL Nuxt 3~~ | Resolved: upgraded to Nuxt 4 + @nuxt/content v3 in `83086af` |

---

## 8. Prior art referenced

- PCPartPicker (rule-based compatibility with error/warning/note severities), keeb-finder builder (URL-encoded builds, no backend), KeebDepot (compatibility matrix), Gundamaxing / Bandai Builders Note (build showcase pages and ratings).
- Tamiya: chassis select guide, GUP matching list (GIF), gear matching list, setup guide, TAMIYA PASSPORT (events only), Mini 4WD Station (locator only).
- ミニ四駆 超速グランプリ (700+ scanned parts, 2D list + 3D car UX; service ended 2024-05-07).
- 4x4 Builder (open-source R3F vehicle configurator with URL config), BrickLink Studio (socket-graph part library).
- Japanese community: TEA-League, mini4wd.tech, ミニ四駆改造マニュアル@wiki, miniyonfan, mini4-masters. Traditional Chinese: TMU 台灣迷你四驅車聯盟, 台灣田宮四驅車同好會, HKM4A 香港迷你四驅協會, tamiya.hk setting guide, blog.hahasmile.com beginner glossary.

Full research notes with source URLs: `docs/research/concepts-and-rules.md`, `docs/research/data-sources.md`, `docs/research/3d-assets.md`, `docs/research/tech-stack.md`.
